import path from"node:path";import{collectFiles,exists,readJson,shaFile,WisePPTError}from"./common.mjs";function isGitMetadata(relative){return relative===".git"||relative.startsWith(".git/")}async function verifyBundle(root){const manifestPath=path.join(root,"bundle-manifest.json");if(!await exists(manifestPath)){if(await exists(path.join(root,".git")))return{mode:"development",source_commit:null,files:0};throw new WisePPTError("缺少 bundle-manifest.json：当前目录不是完整 Wise PPT 发布包")}const manifest=await readJson(manifestPath,"bundle-manifest");if(manifest.format!=="wise-ppt-skill-bundle@2"||JSON.stringify(manifest.node?.supported_majors)!==JSON.stringify([22,24])||!Array.isArray(manifest.files)||!manifest.files.length)throw new WisePPTError("bundle-manifest 合同错误");const seen=new Set;for(const[offset,record]of manifest.files.entries()){const label=`bundle-manifest.files[${offset+1}]`;if(!record||typeof record!="object"||Object.keys(record).sort().join(",")!=="bytes,path,sha256")throw new WisePPTError(`${label} 字段非法`);const relative=record.path;if(typeof relative!="string"||!relative||relative.startsWith("/")||relative.includes("\\")||relative.split("/").some(part=>["",".",".."].includes(part))||seen.has(relative))throw new WisePPTError(`${label}.path 非法或重复`);seen.add(relative);const target=path.join(root,...relative.split("/")),digest=await shaFile(target).catch(()=>null);if(!digest||digest.sha256!==record.sha256||digest.bytes!==record.bytes)throw new WisePPTError(`Wise PPT 发布包文件缺失或漂移: ${relative}`)}const actual=(await collectFiles(root,{includeHidden:!0,exclude:relative=>relative==="bundle-manifest.json"||isGitMetadata(relative)})).sort(),expected=[...seen].sort();if(actual.length!==expected.length||actual.some((item,index)=>item!==expected[index]))throw new WisePPTError("Wise PPT 发布包文件集合与 bundle-manifest 不一致");return{mode:"release",source_commit:manifest.source_commit,files:manifest.files.length}}export{verifyBundle};
