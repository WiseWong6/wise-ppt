import{createHash}from"node:crypto";import{constants as fsConstants}from"node:fs";import{access,copyFile,lstat,mkdir,open,readFile,readdir,rename,rm,stat,writeFile}from"node:fs/promises";import os from"node:os";import path from"node:path";import{fileURLToPath}from"node:url";class WisePPTError extends Error{constructor(message){super(message),this.name="WisePPTError"}}function runtimeRoot(metaUrl=import.meta.url){const current=path.dirname(fileURLToPath(metaUrl));return path.basename(current)==="bin"?path.dirname(current):path.resolve(current,"..","..")}function sha256Bytes(value){return createHash("sha256").update(value).digest("hex")}function sha256Text(value){return sha256Bytes(Buffer.from(value,"utf8"))}async function shaFile(filePath){const bytes=await readFile(filePath);return{sha256:sha256Bytes(bytes),bytes:bytes.length}}function canonicalJson(value){return value===void 0?"null":Array.isArray(value)?`[${value.map(canonicalJson).join(",")}]`:value&&typeof value=="object"?`{${Object.keys(value).sort().map(key=>`${JSON.stringify(key)}:${canonicalJson(value[key])}`).join(",")}}`:JSON.stringify(value)}function renderJson(value){return`${JSON.stringify(sortKeysDeep(value),null,2)}
`}function sortKeysDeep(value){return Array.isArray(value)?value.map(sortKeysDeep):value&&typeof value=="object"?Object.fromEntries(Object.keys(value).sort().map(key=>[key,sortKeysDeep(value[key])])):value}async function readText(filePath,label=path.basename(filePath)){try{return await readFile(filePath,"utf8")}catch(error){throw new WisePPTError(`无法读取${label}: ${filePath}: ${error.message}`)}}async function readJson(filePath,label=path.basename(filePath)){let value;try{value=JSON.parse(await readFile(filePath,"utf8"))}catch(error){throw new WisePPTError(`${label} 不是合法 JSON: ${filePath}: ${error.message}`)}if(!value||typeof value!="object"||Array.isArray(value))throw new WisePPTError(`${label} 顶层必须是 JSON object`);return assertFiniteNumbers(value,label),value}function assertFiniteNumbers(value,label){if(typeof value=="number"&&!Number.isFinite(value))throw new WisePPTError(`${label} 含 NaN/Infinity 非有限数字`);if(Array.isArray(value))value.forEach((item,index)=>assertFiniteNumbers(item,`${label}[${index}]`));else if(value&&typeof value=="object")for(const[key,child]of Object.entries(value))assertFiniteNumbers(child,`${label}.${key}`)}function assertAbsolute(raw,label){const expanded=raw.startsWith("~/")?path.join(os.homedir(),raw.slice(2)):raw;if(!path.isAbsolute(expanded))throw new WisePPTError(`${label}必须是绝对路径`);return path.resolve(expanded)}async function assertNoSymlinkComponents(target,label){const absolute=path.resolve(target),parsed=path.parse(absolute);let current=parsed.root;for(const part of absolute.slice(parsed.root.length).split(path.sep).filter(Boolean)){current=path.join(current,part);const info=await lstat(current).catch(()=>null);if(info?.isSymbolicLink())throw new WisePPTError(`${label} 路径禁止符号链接: ${current}`);if(!info)break}}async function copyFileSafe(source,target){await mkdir(path.dirname(target),{recursive:!0}),await copyFile(source,target)}async function atomicWrite(target,payload){await mkdir(path.dirname(target),{recursive:!0});const temporary=`${target}.tmp-${process.pid}-${Date.now()}`;try{const handle=await open(temporary,"wx",384);try{await handle.writeFile(payload),await handle.sync()}finally{await handle.close()}await rename(temporary,target)}finally{await rm(temporary,{force:!0}).catch(()=>{})}}async function collectFiles(root,options={}){const{includeHidden=!1,exclude=()=>!1}=options,files=[];async function visit(current){const entries=await readdir(current,{withFileTypes:!0});entries.sort((a,b)=>a.name.localeCompare(b.name,"en"));for(const entry of entries){const absolute=path.join(current,entry.name),relative=path.relative(root,absolute).split(path.sep).join("/");if(!(!includeHidden&&entry.name.startsWith(".")||exclude(relative,entry))){if(entry.isSymbolicLink())throw new WisePPTError(`禁止符号链接: ${relative}`);if(entry.isDirectory())await visit(absolute);else if(entry.isFile())files.push(relative);else throw new WisePPTError(`不支持的文件类型: ${relative}`)}}}return await visit(root),files}async function fileRecord(root,relative){const digest=await shaFile(path.join(root,relative));return{path:relative.split(path.sep).join("/"),...digest}}async function writableDirectory(directory){await mkdir(directory,{recursive:!0}),await access(directory,fsConstants.W_OK);const probe=path.join(directory,`.wise-ppt-write-${process.pid}-${Date.now()}`);return await writeFile(probe,"ok",{flag:"wx"}),await rm(probe,{force:!0}),directory}async function exists(filePath){return stat(filePath).then(()=>!0,()=>!1)}function sleep(milliseconds){return new Promise(resolve=>setTimeout(resolve,milliseconds))}export{WisePPTError,assertAbsolute,assertFiniteNumbers,assertNoSymlinkComponents,atomicWrite,canonicalJson,collectFiles,copyFileSafe,exists,fileRecord,readJson,readText,renderJson,runtimeRoot,sha256Bytes,sha256Text,shaFile,sleep,sortKeysDeep,writableDirectory};
