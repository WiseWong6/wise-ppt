import{load}from"#wise-html";import{mkdir,readdir,rename,rm,stat,writeFile}from"node:fs/promises";import"node:os";import path from"node:path";import{BUILD_CONTRACT,DECK_CONTRACT,DECK_PLAN_CONTRACT,DEFAULT_SIGNATURE,LAYOUT_PLAN_REQUEST_FORMAT,OUTPUT_MARKER,REQUIRED_RUNTIME_FILES,REQUIRED_THEME_FILES,RUNTIME_VERSION}from"./constants.mjs";import{assertAbsolute,assertNoSymlinkComponents,canonicalJson,collectFiles,copyFileSafe,exists,fileRecord,readJson,readText,renderJson,sha256Text,shaFile,WisePPTError}from"./common.mjs";import{copyResolvedFonts,resolveFonts}from"./fonts.mjs";import{ICON_SOURCE_FAMILIES,projectThemeAssetForPublishedAssets,renderThemeCss,resolveDeckTheme,THEME_ENGINE_DESIGN_TOKENS,THEME_TYPE_ROLES,TYPOGRAPHY_MODES}from"./theme.mjs";const ALLOWED_TOP_LEVEL=new Set(["contract","mode","deck","layout_context","sources","must","slides"]),ALLOWED_DECK_FIELDS=new Set(["title","thesis","input_type","theme","typography_mode","lang","signature"]),ALLOWED_LAYOUT_CONTEXT_FIELDS=new Set(["scope","selection_seed","prior_total","usage"]),ALLOWED_LAYOUT_USAGE_FIELDS=new Set(["layout_id","count","last_sequence"]),ALLOWED_SOURCE_FIELDS=new Set(["source_id","title"]),ALLOWED_SLIDE_FIELDS=new Set(["page_id","page_role","layout_id","relation_key","claim","payload","source_refs","source_evidence","must_refs","section_id","section_title","emphasis","layout_override"]),ALLOWED_EMPHASIS_FIELDS=new Set(["target","reason"]),ALLOWED_LAYOUT_OVERRIDE_FIELDS=new Set(["basis","reason"]),ALLOWED_MUST_FIELDS=new Set(["must_id","content","status","page_id","reason","visible_evidence","source_refs"]),ALLOWED_LAYOUT_PLAN_FIELDS=new Set(["format","layout_context","pages"]),ALLOWED_LAYOUT_PLAN_PAGE_FIELDS=new Set(["page_id","page_kind","page_role","relation_key","requires","content_items","selected_layout_id","layout_override"]),PAYLOAD_CATEGORIES=Object.freeze({text:"text",data:"data",icons:"icon"}),PAYLOAD_SURFACES=Object.freeze({text:"text",data:"text",icons:"icon"}),INPUT_TYPES=new Set(["pdf","url","multi-doc","existing-deck","oral","short-text"]),SOURCE_BACKED_INPUT_TYPES=new Set(["pdf","url","multi-doc","existing-deck"]),LAYOUT_OVERRIDE_BASES=new Set(["capacity","binding","primary-support","reading-order","user-continuity"]),SELECTION_SEED_PATTERN=/^[0-9a-f]{32}$/,FORBIDDEN_KEYS=new Set(["css","style","page_css","geometry","structure","components","component","component_id","renderer","renderer_id","svg"]),FIXED_GENERATED_FILES=new Set(["index.html","deck-spec.json","deck-plan.json","source-ledger.json","component-receipts.json","geometry-contracts.json","assets/layouts.css"]),BUILD_FORBIDDEN_KEYS=["unregistered_layout","page_css","handwritten_geometry"],RECEIPT_FORBIDDEN_KEYS=["page_css","handwritten_geometry"],THEME_SEMANTIC_VALUES=Object.freeze({"data-theme-surface":new Set(["paper","recessed","foreground","ink"]),"data-theme-layer":new Set(["foreground"]),"data-theme-corner":new Set(["family"]),"data-theme-line":new Set(["hairline","detail","main","emphasis"]),"data-theme-role":new Set(["structural-outline","focus-outline","hollow-number","reverse"]),"data-theme-table-region":new Set(["header","body","selected"]),"data-theme-component":new Set(["card","panel"])}),LAYOUT_THEME_BINDING_CONTRACT="wise-ppt-layout-theme-bindings@2",LAYOUT_THEME_BINDING_SOURCE=LAYOUT_THEME_BINDING_CONTRACT,VISUAL_TREATMENT_GRAMMAR_CONTRACT="wise-ppt-visual-treatment-grammar@3",LAYOUT_THEME_IDENTITY_SOURCE=LAYOUT_THEME_BINDING_CONTRACT,ICON_SOURCE_PRESENTATION_ATTRIBUTES=Object.freeze(["fill","stroke","stroke-width","stroke-linecap","stroke-linejoin"]),LAYOUT_THEME_SURFACES=new Set(["paper","recessed","foreground"]),LAYOUT_THEME_BINDING_ID=/^[a-z][a-z0-9-]*$/,THEME_SEMANTIC_ID=/^[a-z][a-z0-9]*(?:[._-][a-z0-9]+)*$/,THEME_FURNITURE_SELECTOR=/\.(?:doc|folio|caption)(?![A-Za-z0-9_-])/;function setDifference(left,right){return[...left].filter(value=>!right.has(value))}function assertKnownKeys(value,allowed,label){const unknown=Object.keys(value).filter(key=>!allowed.has(key));if(unknown.length)throw new WisePPTError(`${label} 未登记字段: ${unknown.sort().join(", ")}`)}function assertExactKeys(value,expected,label){if(!value||typeof value!="object"||Array.isArray(value))throw new WisePPTError(`${label} 必须是对象`);const actual=Object.keys(value).sort(),required=[...expected].sort();if(canonicalJson(actual)!==canonicalJson(required))throw new WisePPTError(`${label} 字段必须精确为: ${required.join(", ")}`)}async function loadLayoutThemeBindings(root,layoutIds){const[authority,grammar]=await Promise.all([readJson(path.join(root,"themes/engine/contracts/layout-theme-bindings.json"),"逐版式主题语义绑定"),loadVisualTreatmentGrammar(root)]);if(assertExactKeys(authority,["contract","treatment_contract","projection","geometry_policy","catalog_comparison_policy","identity_policy","invariants","layouts"],"逐版式主题语义绑定"),authority.contract!==LAYOUT_THEME_BINDING_CONTRACT||authority.treatment_contract!==VISUAL_TREATMENT_GRAMMAR_CONTRACT||authority.projection!=="compile-time-exact-count"||authority.geometry_policy!=="preserve-all-layouts")throw new WisePPTError("逐版式主题语义绑定合同错误");if(assertExactKeys(authority.catalog_comparison_policy,["scope","default_color_profile","emphasis_profile","override_field"],"逐版式主题语义绑定.catalog_comparison_policy"),authority.catalog_comparison_policy.scope!=="references/catalog.html"||authority.catalog_comparison_policy.default_color_profile!=="standard"||authority.catalog_comparison_policy.emphasis_profile!=="standard"||authority.catalog_comparison_policy.override_field!=="catalog_comparison_profile")throw new WisePPTError("逐版式主题语义绑定 Catalog 定稿策略错误");if(assertExactKeys(authority.identity_policy,["channel","always_on","paper_ink_resolution","chromatic_theme_resolution","furniture_excluded"],"逐版式主题语义绑定.identity_policy"),authority.identity_policy.channel!=="functional"||authority.identity_policy.always_on!==!0||authority.identity_policy.paper_ink_resolution!=="neutral"||authority.identity_policy.chromatic_theme_resolution!=="theme-color"||authority.identity_policy.furniture_excluded!==!0)throw new WisePPTError("逐版式主题身份策略错误");assertExactKeys(authority.invariants,["geometry_baseline","no_surface_reclassification"],"逐版式主题语义绑定.invariants");const knownLayouts=layoutIds instanceof Set?layoutIds:new Set(layoutIds),normalizeInvariant=name=>{const values=authority.invariants[name];if(!Array.isArray(values)||values.some(value=>typeof value!="string")||new Set(values).size!==values.length)throw new WisePPTError(`逐版式主题语义绑定.invariants.${name} 必须是无重复 layout_id 数组`);const unknown=values.filter(value=>!knownLayouts.has(value));if(unknown.length)throw new WisePPTError(`逐版式主题语义绑定.invariants.${name} 含未知 layout_id: ${unknown.join(", ")}`);return[...values]},invariants={geometry_baseline:normalizeInvariant("geometry_baseline"),no_surface_reclassification:normalizeInvariant("no_surface_reclassification")};if(!authority.layouts||typeof authority.layouts!="object"||Array.isArray(authority.layouts))throw new WisePPTError("逐版式主题语义绑定.layouts 必须是对象");const missingLayouts=[...knownLayouts].filter(layoutId=>!Object.hasOwn(authority.layouts,layoutId));if(missingLayouts.length||Object.keys(authority.layouts).length!==knownLayouts.size)throw new WisePPTError(`逐版式主题身份必须覆盖全部骨架；缺少: ${missingLayouts.join(", ")||"-"}`);const byLayout=new Map;for(const[layoutId,entry]of Object.entries(authority.layouts)){if(!knownLayouts.has(layoutId))throw new WisePPTError(`逐版式主题语义绑定含未知 layout_id: ${layoutId}`);if(byLayout.has(layoutId))throw new WisePPTError(`逐版式主题语义绑定 layout_id 重复: ${layoutId}`);if(assertKnownKeys(entry,new Set(["surface_bindings","type_bindings","identity_groups","catalog_comparison_profile"]),`逐版式主题语义绑定.${layoutId}`),!Array.isArray(entry.surface_bindings)||!Array.isArray(entry.type_bindings)||!Array.isArray(entry.identity_groups))throw new WisePPTError(`${layoutId} 的主题绑定必须是数组`);if(entry.catalog_comparison_profile!==void 0&&(assertExactKeys(entry.catalog_comparison_profile,["default_color","emphasis"],`${layoutId}.catalog_comparison_profile`),!["legacy","standard"].includes(entry.catalog_comparison_profile.default_color)||!["legacy","standard"].includes(entry.catalog_comparison_profile.emphasis)))throw new WisePPTError(`${layoutId}.catalog_comparison_profile 只能使用 legacy/standard`);const bindingIds=new Set,surfaceBindings=entry.surface_bindings.map((binding,offset)=>{const label=`${layoutId}.surface_bindings[${offset+1}]`;if(assertExactKeys(binding,["binding_id","selector","expected_count","surface","layer"],label),!LAYOUT_THEME_BINDING_ID.test(String(binding.binding_id||""))||bindingIds.has(binding.binding_id))throw new WisePPTError(`${label}.binding_id 非法或重复`);if(bindingIds.add(binding.binding_id),plainString(binding.selector,`${label}.selector`),positiveInteger(binding.expected_count,`${label}.expected_count`),!LAYOUT_THEME_SURFACES.has(binding.surface))throw new WisePPTError(`${label}.surface 未登记: ${binding.surface}`);if(binding.layer!==null&&binding.layer!=="foreground")throw new WisePPTError(`${label}.layer 只能是 null 或 foreground`);if(binding.layer==="foreground"&&binding.surface!=="foreground")throw new WisePPTError(`${label} foreground layer 必须对应 foreground surface`);return structuredClone(binding)}),typeBindings=entry.type_bindings.map((binding,offset)=>{const label=`${layoutId}.type_bindings[${offset+1}]`;if(assertExactKeys(binding,["binding_id","selector","expected_count","role"],label),!LAYOUT_THEME_BINDING_ID.test(String(binding.binding_id||""))||bindingIds.has(binding.binding_id))throw new WisePPTError(`${label}.binding_id 非法或重复`);if(bindingIds.add(binding.binding_id),plainString(binding.selector,`${label}.selector`),positiveInteger(binding.expected_count,`${label}.expected_count`),!THEME_TYPE_ROLES.includes(binding.role))throw new WisePPTError(`${label}.role 未登记: ${binding.role}`);return structuredClone(binding)});if(entry.identity_groups.length<grammar.constraints.identity_groups_per_page.min||entry.identity_groups.length>grammar.constraints.identity_groups_per_page.max)throw new WisePPTError(`${layoutId} identity group 数量必须为 ${grammar.constraints.identity_groups_per_page.min}–${grammar.constraints.identity_groups_per_page.max}`);const groupIds=new Set,memberKeys=new Set,identityGroups=entry.identity_groups.map((group,groupOffset)=>{const groupLabel=`${layoutId}.identity_groups[${groupOffset+1}]`;if(assertExactKeys(group,["group_id","label","members"],groupLabel),!THEME_SEMANTIC_ID.test(String(group.group_id||""))||groupIds.has(group.group_id))throw new WisePPTError(`${groupLabel}.group_id 非法或重复`);if(groupIds.add(group.group_id),plainString(group.label,`${groupLabel}.label`),!Array.isArray(group.members)||!group.members.length)throw new WisePPTError(`${groupLabel}.members 必须是非空数组`);const members=group.members.map((member,memberOffset)=>{const memberLabel=`${groupLabel}.members[${memberOffset+1}]`;if(assertExactKeys(member,["selector","treatment","expected_count"],memberLabel),plainString(member.selector,`${memberLabel}.selector`),positiveInteger(member.expected_count,`${memberLabel}.expected_count`),THEME_FURNITURE_SELECTOR.test(member.selector))throw new WisePPTError(`${memberLabel} 不得命中 .doc/.folio/.caption 家具`);if(!grammar.identityTreatments.has(member.treatment))throw new WisePPTError(`${memberLabel}.treatment 未登记: ${member.treatment}`);const memberKey=`${member.selector}\0${member.treatment}`;if(memberKeys.has(memberKey))throw new WisePPTError(`${memberLabel} selector+treatment 重复`);return memberKeys.add(memberKey),structuredClone(member)});return{group_id:group.group_id,label:group.label,members}});byLayout.set(layoutId,{surface_bindings:surfaceBindings,type_bindings:typeBindings,identity_groups:identityGroups})}for(const layoutId of invariants.no_surface_reclassification)if((byLayout.get(layoutId)?.surface_bindings||[]).length)throw new WisePPTError(`${layoutId} 已锁定为不重分类材料，不得登记 surface binding`);return{contract:authority.contract,treatmentContract:authority.treatment_contract,invariants,byLayout}}async function loadVisualTreatmentGrammar(root){const grammar=await readJson(path.join(root,"themes/engine/contracts/visual-treatment-grammar.json"),"视觉处理语法");if(assertExactKeys(grammar,["contract","description","color_channels","constraints","identity_treatments","focus_treatments"],"视觉处理语法"),grammar.contract!==VISUAL_TREATMENT_GRAMMAR_CONTRACT)throw new WisePPTError("视觉处理语法版本错误");assertExactKeys(grammar.color_channels,["identity","ink_primary","focus_primary","focus_secondary","focus_peripheral"],"视觉处理语法.color_channels"),assertExactKeys(grammar.constraints,["identity_groups_per_page","active_focus_groups_per_page","focus_target_members_fixed","fourth_color_forbidden","furniture_excluded","overlap_rule"],"视觉处理语法.constraints");for(const[key,expected]of[["identity_groups_per_page",{min:1,max:2}],["active_focus_groups_per_page",{min:0,max:1}]])if(assertExactKeys(grammar.constraints[key],["min","max"],`视觉处理语法.constraints.${key}`),canonicalJson(grammar.constraints[key])!==canonicalJson(expected))throw new WisePPTError(`视觉处理语法.constraints.${key} 错误`);if(grammar.constraints.focus_target_members_fixed!==!0||grammar.constraints.fourth_color_forbidden!==!0||canonicalJson(grammar.constraints.furniture_excluded)!==canonicalJson([".doc",".folio",".caption"]))throw new WisePPTError("视觉处理语法核心约束错误");const normalizeTreatments=(entries,prefix,label)=>{if(!entries||typeof entries!="object"||Array.isArray(entries))throw new WisePPTError(`${label} 必须是对象`);const ids=new Set;for(const[treatment,definition]of Object.entries(entries)){if(!treatment.startsWith(`${prefix}.`)||!THEME_SEMANTIC_ID.test(treatment))throw new WisePPTError(`${label} 含非法 treatment: ${treatment}`);if(assertExactKeys(definition,["channel","effect"],`${label}.${treatment}`),plainString(definition.channel,`${label}.${treatment}.channel`),!Array.isArray(definition.effect)||!definition.effect.length||definition.effect.some(item=>typeof item!="string"||!item.trim()))throw new WisePPTError(`${label}.${treatment}.effect 必须是非空字符串数组`);ids.add(treatment)}if(!ids.size)throw new WisePPTError(`${label} 不得为空`);return ids};return{...grammar,identityTreatments:normalizeTreatments(grammar.identity_treatments,"identity","视觉处理语法.identity_treatments"),focusTreatments:normalizeTreatments(grammar.focus_treatments,"focus","视觉处理语法.focus_treatments")}}function plainString(value,label,allowEmpty=!1){if(typeof value!="string"||!allowEmpty&&!value.trim())throw new WisePPTError(`${label}必须是${allowEmpty?"可空":"非空"}字符串`);return value}function positiveInteger(value,label){if(!Number.isSafeInteger(value)||value<1)throw new WisePPTError(`${label} 必须是正整数`);return value}function nonnegativeInteger(value,label){if(!Number.isSafeInteger(value)||value<0)throw new WisePPTError(`${label} 必须是非负整数`);return value}function compareAscii(left,right){return left<right?-1:left>right?1:0}function normalizeSelectionSeed(value,label="selection_seed"){if(typeof value!="string"||!SELECTION_SEED_PATTERN.test(value))throw new WisePPTError(`${label} 必须是 32 位小写十六进制字符串`);return value}function registryIndex(registry){return new Map((registry.layouts||[]).map((layout,index)=>[layout.layout_id,index]))}function normalizeUsageEntries(entries,layoutIds,label,options={}){if(!Array.isArray(entries))throw new WisePPTError(`${label} 必须是数组`);const seen=new Set,usage=entries.map((entry,offset)=>{const entryLabel=`${label}[${offset+1}]`;if(!entry||typeof entry!="object"||Array.isArray(entry))throw new WisePPTError(`${entryLabel} 必须是对象`);assertKnownKeys(entry,ALLOWED_LAYOUT_USAGE_FIELDS,entryLabel);const layoutId=plainString(entry.layout_id,`${entryLabel}.layout_id`);if(!layoutIds.has(layoutId))throw new WisePPTError(`${entryLabel}.layout_id 未登记: ${layoutId}`);if(seen.has(layoutId))throw new WisePPTError(`${label} 含重复 layout_id: ${layoutId}`);return seen.add(layoutId),{layout_id:layoutId,count:positiveInteger(entry.count,`${entryLabel}.count`),last_sequence:positiveInteger(entry.last_sequence,`${entryLabel}.last_sequence`)}}),derivedTotal=usage.reduce((sum,entry)=>{const next=sum+entry.count;if(!Number.isSafeInteger(next))throw new WisePPTError(`${label} 的 count 总和超过安全整数范围`);return next},0),total=options.priorTotal===void 0?derivedTotal:nonnegativeInteger(options.priorTotal,`${options.contextLabel||label}.prior_total`);if(derivedTotal!==total)throw new WisePPTError(`${label} 的 count 总和必须等于 prior_total=${total}`);const lastSequences=new Set;for(const entry of usage){if(entry.last_sequence>total)throw new WisePPTError(`${label} 的 last_sequence 不得大于 prior_total=${total}`);if(entry.last_sequence<entry.count)throw new WisePPTError(`${label} 的 last_sequence 不得小于 count`);if(lastSequences.has(entry.last_sequence))throw new WisePPTError(`${label} 的 last_sequence 不得重复`);lastSequences.add(entry.last_sequence)}if(total===0&&usage.length||total>0&&Math.max(...lastSequences)!==total)throw new WisePPTError(`${label} 的最大 last_sequence 必须等于 prior_total=${total}`);let cumulativeCount=0;for(const entry of[...usage].sort((left,right)=>left.last_sequence-right.last_sequence))if(cumulativeCount+=entry.count,cumulativeCount>entry.last_sequence)throw new WisePPTError(`${label} 在 last_sequence=${entry.last_sequence} 前无法容纳累计 count=${cumulativeCount}`);const sorted=[...usage].sort((left,right)=>compareAscii(left.layout_id,right.layout_id));if(options.requireSorted&&canonicalJson(usage)!==canonicalJson(sorted))throw new WisePPTError(`${label} 必须按完整 layout_id 升序排列`);return{prior_total:total,usage:sorted}}function normalizeLayoutUsage(registry,entries=[]){return normalizeUsageEntries(entries,new Set((registry.layouts||[]).map(layout=>layout.layout_id)),"layout usage")}function normalizeLayoutContext(value,layoutIds){if(!value||typeof value!="object"||Array.isArray(value))throw new WisePPTError("deck-spec.layout_context 必须是对象");if(assertKnownKeys(value,ALLOWED_LAYOUT_CONTEXT_FIELDS,"deck-spec.layout_context"),value.scope!=="session")throw new WisePPTError("deck-spec.layout_context.scope 必须是 session");const selectionSeed=normalizeSelectionSeed(value.selection_seed,"deck-spec.layout_context.selection_seed"),normalized=normalizeUsageEntries(value.usage,layoutIds,"deck-spec.layout_context.usage",{priorTotal:value.prior_total,contextLabel:"deck-spec.layout_context",requireSorted:!0});return{scope:"session",selection_seed:selectionSeed,...normalized}}function usageMap(normalized){return new Map(normalized.usage.map(entry=>[entry.layout_id,structuredClone(entry)]))}function usageFor(map,layoutId){return map.get(layoutId)||{layout_id:layoutId,count:0,last_sequence:null}}function selectionHash(seed,layoutId){return sha256Text(`${seed}\0${layoutId}`)}function compareByUsage(left,right,usage,seed,order){const leftUsage=usageFor(usage,left.layout_id),rightUsage=usageFor(usage,right.layout_id);if(leftUsage.count!==rightUsage.count)return leftUsage.count-rightUsage.count;const leftLast=leftUsage.last_sequence??-1,rightLast=rightUsage.last_sequence??-1;if(leftLast!==rightLast)return leftLast-rightLast;const hashOrder=compareAscii(selectionHash(seed,left.layout_id),selectionHash(seed,right.layout_id));return hashOrder||order.get(left.layout_id)-order.get(right.layout_id)}function layoutSelectionState(layouts){if(!layouts.length)return"no-candidate";if(layouts.length===1)return"forced-single";const counts=layouts.map(layout=>layout.usage_count);return counts.some(count=>count===0)?"fresh-available":Math.min(...counts)<Math.max(...counts)?"least-used-available":"balanced-reuse"}function semanticValue(value){return typeof value=="string"?!!value.trim():typeof value=="number"?Number.isFinite(value):Array.isArray(value)?value.some(semanticValue):value&&typeof value=="object"?Object.values(value).some(semanticValue):!1}function payloadSemanticValue(value){return value&&typeof value=="object"&&!Array.isArray(value)&&("fields"in value||"items"in value)?semanticValue(value.fields):semanticValue(value)}function referenceList(value,label,allowed,requireNonempty=!1){if(!Array.isArray(value)||!value.every(item=>typeof item=="string"&&item.trim()))throw new WisePPTError(`${label} 必须是由非空字符串组成的数组`);if(requireNonempty&&!value.length)throw new WisePPTError(`${label} 对当前 input_type 必须非空`);if(new Set(value).size!==value.length)throw new WisePPTError(`${label} 不得重复`);const unknown=value.filter(item=>!allowed.has(item));if(unknown.length)throw new WisePPTError(`${label} 含未登记引用: ${unknown.sort().join(", ")}`);return value}function sourceEvidenceMap(value,refs,label){if(!value||typeof value!="object"||Array.isArray(value))throw new WisePPTError(`${label} 必须是 source_id → 可见词条数组的对象`);const expected=new Set(refs),actual=new Set(Object.keys(value));if(expected.size!==actual.size||setDifference(expected,actual).length||setDifference(actual,expected).length)throw new WisePPTError(`${label} 必须与 source_refs 一一对应`);for(const sourceId of refs){const terms=value[sourceId];if(!Array.isArray(terms)||!terms.length||!terms.every(term=>typeof term=="string"&&term.trim()))throw new WisePPTError(`${label}.${sourceId} 必须是非空可见词条数组`);if(new Set(terms).size!==terms.length)throw new WisePPTError(`${label}.${sourceId} 可见词条不得重复`)}return value}function shouldSkipForbiddenTraversal(current){return current==="deck.theme.definition"}function walkForbidden(value,current=""){if(current==="deck.theme_preset"||current==="deck.theme_family")throw new WisePPTError("deck@9 已删除 theme_preset/theme_family；只允许 deck.theme");if(!shouldSkipForbiddenTraversal(current)){if(Array.isArray(value)){value.forEach((child,index)=>walkForbidden(child,`${current}[${index}]`));return}if(!(!value||typeof value!="object"))for(const[key,child]of Object.entries(value)){const normalized=key.toLowerCase().replaceAll("-","_");if(FORBIDDEN_KEYS.has(normalized))throw new WisePPTError(`标准模式禁止字段: ${current?`${current}.`:""}${key}`);normalized==="payload"&&current.startsWith("slides[")||walkForbidden(child,current?`${current}.${key}`:key)}}}function registeredBindings(category,slot){const kind=category==="icons"?"icon":"text",bindings=slot?.payload_schema?.[kind]?.binding_keys;return Array.isArray(bindings)?bindings.filter(item=>item&&typeof item.key=="string"&&item.key):[]}function validateCapacity(slot,count,label){const minimum=slot?.capacity?.min_items,maximum=slot?.capacity?.max_items;if(Number.isInteger(minimum)&&count<minimum)throw new WisePPTError(`${label}.items=${count} 小于骨架最小容量 ${minimum}`);if(Number.isInteger(maximum)&&count>maximum)throw new WisePPTError(`${label}.items=${count} 超过骨架最大容量 ${maximum}；请精简、换骨架或拆页`)}function expectedBindingKeys(bindings,declaredItems,label){const indexes=new Set(bindings.filter(item=>item.item_scope==="item").map(item=>item.item_index));if(!indexes.size||[...indexes].some(index=>!Number.isInteger(index)||index<0))throw new WisePPTError(`${label} 的登记 binding 缺少合法 item grouping`);const available=Math.max(...indexes)+1;if(declaredItems>available)throw new WisePPTError(`${label}.items=${declaredItems} 超过登记 item groups ${available}；请换骨架或拆页`);return new Set(bindings.filter(item=>item.item_scope==="slot"||item.item_scope==="item"&&item.item_index<declaredItems).map(item=>item.key))}function validateExactFields(fields,bindings,items,label){const expected=expectedBindingKeys(bindings,items,label),actual=new Set(Object.keys(fields)),missing=setDifference(expected,actual),extra=setDifference(actual,expected);if(missing.length||extra.length)throw new WisePPTError(`${label}.fields 必须精确填写 declared items 对应的完整 binding：缺失=${missing.sort()}，超出=${extra.sort()}`)}function normalizedExample(value){return["string","number"].includes(typeof value)?String(value).trim().replace(/\s+/g," "):""}function validatePayloadShape(category,value,label,slot){const bindings=registeredBindings(category,slot);if(!bindings.length)throw new WisePPTError(`${label} 没有公开 binding key`);if(Array.isArray(value))throw new WisePPTError(`${label} 禁止位置数组；请按公开 binding key 填写 fields`);if(bindings.length>1&&(!value||typeof value!="object"))throw new WisePPTError(`${label} 是多字段槽，必须使用 fields/items 控制对象`);let fields,items;if(value&&typeof value=="object"){const keys=Object.keys(value);if(keys.length!==2||!keys.includes("fields")||!keys.includes("items"))throw new WisePPTError(`${label} 只能使用 {fields: {binding_key: value}, items: n}`);if(fields=value.fields,items=value.items,!fields||typeof fields!="object"||Array.isArray(fields))throw new WisePPTError(`${label}.fields 必须是 binding key → 值的对象`);if(!Number.isInteger(items)||items<0)throw new WisePPTError(`${label}.items 必须是非负整数`)}else fields={[bindings[0].key]:value},items=1;const known=new Set(bindings.map(item=>item.key)),unknown=Object.keys(fields).filter(key=>!known.has(key));if(unknown.length)throw new WisePPTError(`${label}.fields 使用未知 binding key: ${unknown.sort().join(", ")}`);const empty=Object.entries(fields).filter(([,field])=>!semanticValue(field)).map(([key])=>key);if(empty.length)throw new WisePPTError(`${label}.fields 含空白登记值: ${empty.sort().join(", ")}`);if(validateCapacity(slot,items,label),validateExactFields(fields,bindings,items,label),category!=="icons"){const byKey=new Map(bindings.map(binding=>[binding.key,normalizedExample(binding.example)]));if(Object.keys(fields).length&&Object.entries(fields).every(([key,field])=>byKey.get(key)&&normalizedExample(field)===byKey.get(key)))throw new WisePPTError(`${label} 整槽照抄 Catalog example；至少替换一个字段为真实内容`)}}function validatePayloadExclusivity(payload,pageId){const owners=new Map;for(const category of Object.keys(PAYLOAD_CATEGORIES)){const slots=payload[category];if(!(!slots||typeof slots!="object"||Array.isArray(slots)))for(const slotId of Object.keys(slots)){const key=`${slotId}\0${PAYLOAD_SURFACES[category]}`,previous=owners.get(key);if(previous&&previous!==category)throw new WisePPTError(`${pageId}.payload 同一槽 ${slotId} 的 ${PAYLOAD_SURFACES[category]} binding surface 不能同时由 ${previous} 与 ${category} 填写`);owners.set(key,category)}}}async function attachCapabilityContracts(root,registry){const lockedIconColors=new Set(["var(--wp-color-functional)","var(--ink)","var(--ink-70)","var(--ink-45)","var(--ink-25)"]),[emphasisContract,iconContract,treatmentGrammar]=await Promise.all([readJson(path.join(root,"capabilities/layouts/page-emphasis-contracts.json"),"页面强调合同"),readJson(path.join(root,"capabilities/layouts/icon-slot-contracts.json"),"图标槽位合同"),loadVisualTreatmentGrammar(root)]);if(emphasisContract.contract_version!==4||emphasisContract.capability_id!=="wise-ppt.page-emphasis"||emphasisContract.treatment_contract!==VISUAL_TREATMENT_GRAMMAR_CONTRACT)throw new WisePPTError("页面强调合同版本错误");if(iconContract.contract!=="wise-ppt-icon-slots@2"||iconContract.capability_id!=="wise-ppt.icon-slots")throw new WisePPTError("图标槽位合同版本错误");const generatedSources={layout_registry:"capabilities/layouts/layout-registry.json",component_routing:"capabilities/components/routing-manifest.json",runtime_authority:"capabilities/runtime-authority-manifest.json"};for(const[key,relative]of Object.entries(generatedSources)){const record=iconContract.generated_from?.[key],current=await shaFile(path.join(root,...relative.split("/")));if(record?.path!==relative||record.sha256!==current.sha256)throw new WisePPTError(`图标槽位合同的 ${key} 来源已过期`)}const targetContract=emphasisContract.production_target_contract||{};if(targetContract.reason_required!==!0||targetContract.selector_visibility!=="compiler-private"||targetContract.selection!=="semantic-target-id-only"||targetContract.members_fixed!==!0||targetContract.none_allowed!==!0)throw new WisePPTError("页面强调生产合同必须要求原因并隐藏私有选择器");const allowedMemberRoles=new Set(emphasisContract.allowed_member_roles||[]);if(!allowedMemberRoles.size)throw new WisePPTError("页面强调合同未登记允许的成员角色");if(canonicalJson([...emphasisContract.allowed_treatments||[]].sort())!==canonicalJson([...treatmentGrammar.focusTreatments].sort()))throw new WisePPTError("页面强调 treatment 词表与视觉处理语法不一致");const componentRouting=await readJson(path.join(root,generatedSources.component_routing),"组件路由"),routingComponents=componentRouting.components||[],componentIds=new Set(routingComponents.map(item=>item.component_id));if(!Array.isArray(componentRouting.components)||componentIds.size!==routingComponents.length)throw new WisePPTError("组件路由集合非法或重复");const iconScope=iconContract.allowed_icon_scope||{},sourceFamilies=iconScope.source_families||{},originalPresentation=sourceFamilies["tabler-original-v3.46.0"]?.root_presentation||{},invalidFamily=ICON_SOURCE_FAMILIES.some(family=>{const familyScope=sourceFamilies[family]||{},iconPaint=new Set(familyScope.paint||[]);return iconPaint.size!==2||!iconPaint.has("currentColor")||!iconPaint.has("none")||!(familyScope.stroke_widths||[]).length||familyScope.stroke_widths.some(value=>!Number.isFinite(Number(value))||Number(value)<0)});if(canonicalJson(Object.keys(sourceFamilies).sort())!==canonicalJson([...ICON_SOURCE_FAMILIES].sort())||iconScope.seed_viewBox!=="0 0 64 64"||sourceFamilies["redraw-v3"]?.viewBox!=="0 0 64 64"||sourceFamilies["tabler-original-v3.46.0"]?.viewBox!=="0 0 24 24"||canonicalJson(originalPresentation)!==canonicalJson({fill:"none",stroke:"currentColor","stroke-width":"2","stroke-linecap":"round","stroke-linejoin":"round"})||iconScope.source_hash_required!==!0||invalidFamily)throw new WisePPTError("图标来源、尺寸、线宽或颜色合同非法");const emphasisPages=emphasisContract.pages||{},iconLayouts=iconContract.layouts||{},expectedIds=new Set((registry.layouts||[]).map(layout=>layout.layout_id));if(Object.keys(emphasisPages).length!==expectedIds.size||Object.keys(iconLayouts).length!==expectedIds.size)throw new WisePPTError("强调或图标槽位合同页面集合不闭合");const counts=iconContract.counts||{},relationshipLayouts=(registry.layouts||[]).filter(layout=>layout.page_kind==="relationship").length,nonrelationshipLayouts=(registry.layouts||[]).filter(layout=>layout.page_kind==="nonrelationship").length;if(counts.layouts!==expectedIds.size||counts.relationship_layouts!==relationshipLayouts||counts.nonrelationship_layouts!==nonrelationshipLayouts||counts.components!==componentIds.size)throw new WisePPTError("图标槽位合同的页面或组件计数已过期");const placementsByComponent=new Map([...componentIds].map(componentId=>[componentId,[]]));let registeredIconSlots=0;for(const layout of registry.layouts||[]){const page=emphasisPages[layout.display_code],iconPage=iconLayouts[layout.layout_id];if(!page||page.page_kind!==layout.page_kind)throw new WisePPTError(`${layout.display_code} 页面强调合同缺失或类型错误`);if(!iconPage||iconPage.display_code!==layout.display_code||iconPage.page_kind!==layout.page_kind)throw new WisePPTError(`${layout.display_code} 图标槽位合同缺失或类型错误`);if(assertExactKeys(page,["page_kind","page_name","access","default_mode","primary_relation","relationship_family","focus_candidates","normal_contrast","theme_focus_carriers","targets","sample_target_id","rationale"],`${layout.display_code} 页面强调合同`),!["none","semantic-focus","conditional-semantic-focus","contrast-only"].includes(page.access)||page.default_mode!=="none")throw new WisePPTError(`${layout.display_code} 页面强调权限非法`);const enabled=["semantic-focus","conditional-semantic-focus"].includes(page.access);if(!Array.isArray(page.targets)||enabled&&!page.targets.length||!enabled&&page.targets.length)throw new WisePPTError(`${layout.display_code} 强调候选与权限不一致`);if(enabled&&(!Array.isArray(page.focus_candidates)||!page.focus_candidates.length||page.focus_candidates.some(item=>typeof item!="string"||!item.trim())))throw new WisePPTError(`${layout.display_code} 开放强调但没有可见焦点说明`);const targetIds=new Set,privateTargets=new Map,publicTargets=page.targets.map((target,targetOffset)=>{const targetLabel=`${layout.display_code}.targets[${targetOffset+1}]`;if(assertExactKeys(target,["target_id","label","content_role","reason_hint","members"],targetLabel),!THEME_SEMANTIC_ID.test(String(target.target_id||""))||targetIds.has(target.target_id))throw new WisePPTError(`${targetLabel}.target_id 非法或重复`);if(targetIds.add(target.target_id),plainString(target.label,`${targetLabel}.label`),plainString(target.content_role,`${targetLabel}.content_role`),plainString(target.reason_hint,`${targetLabel}.reason_hint`),!Array.isArray(target.members)||!target.members.length)throw new WisePPTError(`${targetLabel}.members 必须是非空数组`);const memberKeys=new Set,members=target.members.map((member,memberOffset)=>{const memberLabel=`${targetLabel}.members[${memberOffset+1}]`,expectedKeys=member.paint?["selector","role","treatment","expected_count","paint"]:["selector","role","treatment","expected_count"];if(assertExactKeys(member,expectedKeys,memberLabel),plainString(member.selector,`${memberLabel}.selector`),positiveInteger(member.expected_count,`${memberLabel}.expected_count`),!allowedMemberRoles.has(member.role)||!(page.theme_focus_carriers||[]).includes(member.role))throw new WisePPTError(`${memberLabel}.role 不受逐页合同允许`);if(!treatmentGrammar.focusTreatments.has(member.treatment))throw new WisePPTError(`${memberLabel}.treatment 未登记: ${member.treatment}`);if(member.paint!==void 0&&!["fill","stroke"].includes(member.paint))throw new WisePPTError(`${memberLabel}.paint 只能是 fill 或 stroke`);const memberKey=`${member.selector}\0${member.role}\0${member.treatment}`;if(memberKeys.has(memberKey))throw new WisePPTError(`${memberLabel} selector+role+treatment 重复`);return memberKeys.add(memberKey),structuredClone(member)});return privateTargets.set(target.target_id,members),{target_id:target.target_id,label:target.label,content_role:target.content_role,reason_hint:target.reason_hint,member_roles:[...new Set(members.map(member=>member.role))],reason_required:!0}});if(page.sample_target_id!==null&&!targetIds.has(page.sample_target_id))throw new WisePPTError(`${layout.display_code}.sample_target_id 未落到已登记目标`);if(layout.emphasis={access:page.access,default_mode:"none",max_active:1,targets:publicTargets},layout._emphasis_targets=privateTargets,layout.icon_slots=structuredClone(iconPage.icon_slots||[]),!Array.isArray(layout.icon_slots)||layout.icon_slots.length!==iconPage.icon_slot_count)throw new WisePPTError(`${layout.display_code} 图标槽位计数错误`);const registeredBindings2=new Set;for(const slot of layout.slots||[])for(const binding of slot.payload_schema?.icon?.binding_keys||[]){const bindingId=`${slot.slot_id}\0${binding.key}`;if(registeredBindings2.has(bindingId))throw new WisePPTError(`${layout.display_code}/${binding.key} 图标 binding 重复`);registeredBindings2.add(bindingId)}const contractBindings=new Set;for(const iconSlot of layout.icon_slots){const slot=(layout.slots||[]).find(item=>item.slot_id===iconSlot.slot_id),keys=new Set(slot?.payload_schema?.icon?.binding_keys?.map(item=>item.key)||[]),bindingId=`${iconSlot.slot_id}\0${iconSlot.binding_key}`,geometry=iconSlot.position?.geometry||{},componentId=iconSlot.component_id;if(contractBindings.has(bindingId))throw new WisePPTError(`${layout.display_code}/${iconSlot.binding_key} 图标槽位重复`);if(contractBindings.add(bindingId),!slot||!keys.has(iconSlot.binding_key)||iconSlot.layout_id!==layout.layout_id||iconSlot.display_code!==layout.display_code||typeof iconSlot.semantic_purpose!="string"||iconSlot.semantic_purpose!==slot.purpose||!componentIds.has(componentId)||!(layout.locks?.core_component_ids||[]).includes(componentId))throw new WisePPTError(`${layout.display_code}/${iconSlot.binding_key} 图标槽位未落入当前 layout binding 或组件路由`);if(!Array.isArray(iconSlot.position?.fit_box)||iconSlot.position?.target_tag!=="svg"||!["x","y","width","height"].every(key=>typeof geometry[key]=="string"&&Number.isFinite(Number(geometry[key])))||Number(geometry.width)<=0||Number(geometry.width)!==Number(geometry.height)||geometry.viewBox!==iconScope.seed_viewBox||iconSlot.visual_lock?.size_and_position!=="seed-locked"||!lockedIconColors.has(iconSlot.visual_lock?.color)||iconSlot.visual_lock?.stroke_and_shape!=="selected-authority-svg-locked")throw new WisePPTError(`${layout.display_code}/${iconSlot.binding_key} 图标的位置、尺寸、颜色或线条锁定非法`);placementsByComponent.get(componentId).push({layout_id:layout.layout_id,display_code:layout.display_code,slot_id:iconSlot.slot_id,binding_key:iconSlot.binding_key})}const missingBindings=setDifference(registeredBindings2,contractBindings),extraBindings=setDifference(contractBindings,registeredBindings2);if(missingBindings.length||extraBindings.length)throw new WisePPTError(`${layout.display_code} 图标 binding 与槽位合同不一致`);registeredIconSlots+=layout.icon_slots.length}if(counts.registered_icon_slots!==registeredIconSlots)throw new WisePPTError("图标槽位合同的总槽位计数错误");const componentContracts=iconContract.components||{};if(Object.keys(componentContracts).length!==componentIds.size||setDifference(componentIds,new Set(Object.keys(componentContracts))).length)throw new WisePPTError("图标槽位合同的组件集合不闭合");for(const component of routingComponents){const contract=componentContracts[component.component_id],expectedPlacements=placementsByComponent.get(component.component_id),actualPlacements=Array.isArray(contract?.registered_placements)?contract.registered_placements:[],expectedSet=new Set(expectedPlacements.map(item=>canonicalJson(item))),actualSet=new Set(actualPlacements.map(item=>canonicalJson(item)));if(!contract||contract.component_name!==component.name||contract.icon_slot_count!==expectedPlacements.length||expectedSet.size!==expectedPlacements.length||actualSet.size!==actualPlacements.length||setDifference(expectedSet,actualSet).length||setDifference(actualSet,expectedSet).length)throw new WisePPTError(`${component.component_id} 组件图标槽位投影已过期`)}return{emphasisContract,iconContract}}async function registryState(root){const file=path.join(root,"capabilities/layouts/layout-registry.json"),registry=await readJson(file,"Wise PPT 骨架注册表");if(registry.contract_version!==3||registry.registry_id!=="wise-ppt.layouts")throw new WisePPTError("Wise PPT 骨架注册表版本错误");if(!registry.counts||registry.counts.total!==88||registry.counts.relationship!==75||registry.counts.nonrelationship!==13)throw new WisePPTError(`Wise PPT 骨架注册表数量错误: ${JSON.stringify(registry.counts)}`);if(!Array.isArray(registry.layouts)||registry.layouts.length!==registry.counts.total)throw new WisePPTError("Wise PPT 骨架注册表 layouts 数量错误");await attachCapabilityContracts(root,registry);const index=new Map;for(const layout of registry.layouts){if(!layout.layout_id||index.has(layout.layout_id))throw new WisePPTError(`Wise PPT 注册表 layout_id 缺失或重复: ${layout.layout_id||""}`);index.set(layout.layout_id,layout)}return{registry,index,sha256:(await shaFile(file)).sha256}}async function resolvedAppearance(root,deck){if(Object.hasOwn(deck,"theme_preset")||Object.hasOwn(deck,"theme_family"))throw new WisePPTError("deck@9 已删除 theme_preset/theme_family；只允许 deck.theme");const theme=await resolveDeckTheme(root,deck.theme),typography=String(deck.typography_mode||theme.typography.default_mode);if(!TYPOGRAPHY_MODES.includes(typography))throw new WisePPTError(`未登记 typography_mode: ${typography}`);return{theme,themeId:theme.theme_id,typography}}function hardLayoutCandidates(index,slide,chosenLayout){const requiredPayloadTypes=new Set(Object.keys(slide.payload||{}).map(category=>PAYLOAD_CATEGORIES[category]).filter(Boolean));return[...index.values()].filter(layout=>!(layout.page_kind!==chosenLayout.page_kind||!((layout.page_kind==="relationship"?layout.page_roles:[layout.page_role])||[]).includes(slide.page_role)||layout.page_kind==="relationship"&&!(layout.relations||[]).includes(slide.relation_key)||[...requiredPayloadTypes].some(type=>!(layout.allowed_payload_types||[]).includes(type))))}function normalizedLayoutOverride(slide,requiresOverride,selectionRank,preferredLayoutId){if(slide.layout_override!==void 0){if(!slide.layout_override||typeof slide.layout_override!="object"||Array.isArray(slide.layout_override))throw new WisePPTError(`${slide.page_id}.layout_override 必须是对象`);assertKnownKeys(slide.layout_override,ALLOWED_LAYOUT_OVERRIDE_FIELDS,`${slide.page_id}.layout_override`);const basis=plainString(slide.layout_override.basis,`${slide.page_id}.layout_override.basis`);if(!LAYOUT_OVERRIDE_BASES.has(basis))throw new WisePPTError(`${slide.page_id}.layout_override.basis 未登记: ${basis}`);plainString(slide.layout_override.reason,`${slide.page_id}.layout_override.reason`)}if(requiresOverride&&slide.layout_override===void 0)throw new WisePPTError(`${slide.page_id} 选择的 ${slide.layout_id} 在硬候选排序中为第 ${selectionRank}，首选为 ${preferredLayoutId}；必须填写 layout_override`);if(!requiresOverride&&slide.layout_override!==void 0)throw new WisePPTError(`${slide.page_id}.layout_override 没有越过排序更靠前的硬候选，不得填写`);return slide.layout_override?structuredClone(slide.layout_override):null}function buildLayoutSessionReceipt(resolved,index,layoutContext){const currentUsage=usageMap(layoutContext),order=new Map([...index.keys()].map((layoutId,offset)=>[layoutId,offset])),postTotal=layoutContext.prior_total+resolved.length;if(!Number.isSafeInteger(postTotal))throw new WisePPTError("layout session post_total 超过安全整数范围");const pages=[];for(const[offset,{slide,layout}]of resolved.entries()){const candidates=hardLayoutCandidates(index,slide,layout),ranked=[...candidates].sort((left,right)=>compareByUsage(left,right,currentUsage,layoutContext.selection_seed,order)),selectedBefore=usageFor(currentUsage,layout.layout_id),minimumUsage=Math.min(...candidates.map(candidate=>usageFor(currentUsage,candidate.layout_id).count)),selectionRank=ranked.findIndex(candidate=>candidate.layout_id===layout.layout_id)+1,requiresOverride=selectionRank>1,layoutOverride=normalizedLayoutOverride(slide,requiresOverride,selectionRank,ranked[0].layout_id),enrichedCandidates=candidates.map(candidate=>({usage_count:usageFor(currentUsage,candidate.layout_id).count}));pages.push({page_id:slide.page_id,layout_id:layout.layout_id,candidate_count:candidates.length,chosen_usage_before:selectedBefore.count,chosen_last_sequence_before:selectedBefore.last_sequence,minimum_usage_before:minimumUsage,selection_rank:selectionRank,preferred_layout_id:ranked[0].layout_id,decision_type:layoutSelectionState(enrichedCandidates),layout_override:layoutOverride});const sequence=layoutContext.prior_total+offset+1;currentUsage.set(layout.layout_id,{layout_id:layout.layout_id,count:selectedBefore.count+1,last_sequence:sequence})}const postUsage=[...currentUsage.values()].sort((left,right)=>compareAscii(left.layout_id,right.layout_id));return{scope:"session",selection_seed:layoutContext.selection_seed,order_basis:["usage-count-asc","last-sequence-asc","session-seed-hash","registry-order"],prior_total:layoutContext.prior_total,prior_usage:structuredClone(layoutContext.usage),post_total:postTotal,post_usage:postUsage,pages}}function preflightCollector(){const errors=[],seen=new Set,add=(code,pathName,pageId,error)=>{if(!(error instanceof WisePPTError))throw error;const issue={code,page_id:pageId||null,path:pathName,message:error.message},key=canonicalJson(issue);seen.has(key)||(seen.add(key),errors.push(issue))};return{errors,add,check:(code,pathName,pageId,operation,fallback=null)=>{try{return operation()}catch(error){return add(code,pathName,pageId,error),fallback}},checkAsync:async(code,pathName,pageId,operation,fallback=null)=>{try{return await operation()}catch(error){return add(code,pathName,pageId,error),fallback}}}}function forbiddenSpecPaths(value,current="",found=[]){if(shouldSkipForbiddenTraversal(current))return found;if(Array.isArray(value))return value.forEach((child,index)=>forbiddenSpecPaths(child,`${current}[${index}]`,found)),found;if(!value||typeof value!="object")return found;for(const[key,child]of Object.entries(value)){const normalized=key.toLowerCase().replaceAll("-","_"),keyPath=current?`${current}.${key}`:key;FORBIDDEN_KEYS.has(normalized)?found.push(keyPath):normalized==="payload"&&current.startsWith("slides[")||forbiddenSpecPaths(child,keyPath,found)}return found}function assertKnownPreflightKeys(value,allowed,label){const unknown=Object.keys(value).filter(key=>{const normalized=key.toLowerCase().replaceAll("-","_");return!allowed.has(key)&&!FORBIDDEN_KEYS.has(normalized)});if(unknown.length)throw new WisePPTError(`${label} 未登记字段: ${unknown.sort().join(", ")}`)}function collectLayoutSessionIssues(resolved,index,layoutContext,collector){const currentUsage=usageMap(layoutContext),order=new Map([...index.keys()].map((layoutId,offset)=>[layoutId,offset])),postTotal=layoutContext.prior_total+resolved.length;if(!Number.isSafeInteger(postTotal)){collector.add("layout-session.total","layout_context.prior_total",null,new WisePPTError("layout session post_total 超过安全整数范围"));return}for(const[offset,{slide,layout,slideOffset}]of resolved.entries()){const ranked=[...hardLayoutCandidates(index,slide,layout)].sort((left,right)=>compareByUsage(left,right,currentUsage,layoutContext.selection_seed,order)),selectionRank=ranked.findIndex(candidate=>candidate.layout_id===layout.layout_id)+1;if(selectionRank>0){collector.check("layout.override-invalid",`slides[${slideOffset+1}].layout_override`,slide.page_id,()=>normalizedLayoutOverride(slide,selectionRank>1,selectionRank,ranked[0].layout_id));const before=usageFor(currentUsage,layout.layout_id);currentUsage.set(layout.layout_id,{layout_id:layout.layout_id,count:before.count+1,last_sequence:layoutContext.prior_total+offset+1})}}}async function preflightSpec(root,spec,layoutIndex=null){const collector=preflightCollector(),{add,check,checkAsync}=collector;if(!(spec&&typeof spec=="object"&&!Array.isArray(spec)))return add("spec.object-required","$",null,new WisePPTError("deck-spec 顶层必须是对象")),{status:"fail",error_count:collector.errors.length,errors:collector.errors};check("spec.unknown-field","$",null,()=>assertKnownPreflightKeys(spec,ALLOWED_TOP_LEVEL,"deck-spec 顶层")),spec.contract!==DECK_CONTRACT&&add("spec.contract","contract",null,new WisePPTError(`deck-spec.contract 必须是 ${DECK_CONTRACT}`)),(spec.mode||"standard")!=="standard"&&add("spec.mode","mode",null,new WisePPTError("公开 build 只接受 mode=standard；实验骨架不能混入标准成品"));for(const forbiddenPath of forbiddenSpecPaths(spec))add("spec.forbidden-field",forbiddenPath,null,new WisePPTError(`标准模式禁止字段: ${forbiddenPath}`));const{index}=layoutIndex?{index:layoutIndex}:await registryState(root),layoutContext=check("layout-context.invalid","layout_context",null,()=>normalizeLayoutContext(spec.layout_context,new Set(index.keys()))),deck=spec.deck&&typeof spec.deck=="object"&&!Array.isArray(spec.deck)?spec.deck:null;let inputType=null;deck?(check("deck.unknown-field","deck",null,()=>assertKnownPreflightKeys(deck,ALLOWED_DECK_FIELDS,"deck-spec.deck")),check("deck.title","deck.title",null,()=>plainString(deck.title,"deck.title")),check("deck.thesis","deck.thesis",null,()=>plainString(deck.thesis,"deck.thesis")),inputType=check("deck.input-type","deck.input_type",null,()=>plainString(deck.input_type,"deck.input_type")),inputType&&!INPUT_TYPES.has(inputType)&&add("deck.input-type","deck.input_type",null,new WisePPTError(`deck.input_type 未登记: ${inputType}`)),deck.typography_mode!==void 0&&check("deck.typography","deck.typography_mode",null,()=>plainString(deck.typography_mode,"deck.typography_mode")),deck.lang!==void 0&&check("deck.lang","deck.lang",null,()=>{if(!/^[A-Za-z]{2,8}(?:-[A-Za-z0-9]{1,8})*$/.test(plainString(deck.lang,"deck.lang")))throw new WisePPTError("deck.lang 必须是受限 ASCII language tag")}),deck.signature!==void 0&&check("deck.signature","deck.signature",null,()=>{if(!plainString(deck.signature,"deck.signature").trim())throw new WisePPTError("deck.signature 必须是非空白署名")}),await checkAsync("deck.appearance","deck.theme",null,()=>resolvedAppearance(root,deck))):add("deck.object-required","deck",null,new WisePPTError("deck-spec.deck 必须是对象"));const sourceBacked=SOURCE_BACKED_INPUT_TYPES.has(inputType),sourceIds=new Set;let referencesReliable=!0;Array.isArray(spec.sources)?(sourceBacked&&!spec.sources.length&&add("sources.required","sources",null,new WisePPTError(`deck.input_type=${inputType} 必须登记至少一个 source`)),spec.sources.forEach((source,offset)=>{const label=`sources[${offset+1}]`;if(!source||typeof source!="object"||Array.isArray(source)){add("source.object-required",label,null,new WisePPTError(`${label} 必须是对象`)),referencesReliable=!1;return}check("source.unknown-field",label,null,()=>assertKnownPreflightKeys(source,ALLOWED_SOURCE_FIELDS,label));const sourceId=check("source.id",`${label}.source_id`,null,()=>plainString(source.source_id,`${label}.source_id`));check("source.title",`${label}.title`,null,()=>plainString(source.title,`${label}.title`)),sourceId?sourceIds.has(sourceId)?(add("source.duplicate-id",`${label}.source_id`,null,new WisePPTError(`source_id 重复: ${sourceId}`)),referencesReliable=!1):sourceIds.add(sourceId):referencesReliable=!1})):add("sources.array-required","sources",null,new WisePPTError("deck-spec.sources 必须是数组"));const usedSourceIds=new Set,mustById=new Map;if(!Array.isArray(spec.must))add("must.array-required","must",null,new WisePPTError("deck-spec.must 必须是数组"));else for(const[offset,item]of spec.must.entries()){const label=`must[${offset+1}]`;if(!item||typeof item!="object"||Array.isArray(item)){add("must.object-required",label,null,new WisePPTError(`${label} 必须是对象`));continue}check("must.unknown-field",label,null,()=>assertKnownPreflightKeys(item,ALLOWED_MUST_FIELDS,label));const mustId=check("must.id",`${label}.must_id`,null,()=>plainString(item.must_id,`${label}.must_id`));mustId&&mustById.has(mustId)&&add("must.duplicate-id",`${label}.must_id`,null,new WisePPTError(`must_id 重复: ${mustId}`)),check("must.content",`${label}.content`,null,()=>plainString(item.content,`${label}.content`));const status=check("must.status",`${label}.status`,null,()=>plainString(item.status,`${label}.status`));status&&!["placed","omitted"].includes(status)&&add("must.status",`${label}.status`,null,new WisePPTError(`${label}.status 只能是 placed 或 omitted`));const refs=check("must.source-refs",`${label}.source_refs`,null,()=>referenceList(item.source_refs,`${label}.source_refs`,sourceIds,sourceBacked));refs?refs.forEach(id=>usedSourceIds.add(id)):referencesReliable=!1,status==="placed"?(check("must.page-id",`${label}.page_id`,null,()=>plainString(item.page_id,`${label}.page_id`)),check("must.visible-evidence",`${label}.visible_evidence`,null,()=>plainString(item.visible_evidence,`${label}.visible_evidence`)),[void 0,null,""].includes(item.reason)||add("must.placed-reason",`${label}.reason`,null,new WisePPTError(`${label} 已落页，不得声明 omitted reason`))):status==="omitted"&&(check("must.reason",`${label}.reason`,null,()=>plainString(item.reason,`${label}.reason`)),(![void 0,null,""].includes(item.page_id)||![void 0,null,""].includes(item.visible_evidence))&&add("must.omitted-placement",label,null,new WisePPTError(`${label} 已省略，不得声明 page_id/visible_evidence`))),mustId&&!mustById.has(mustId)&&mustById.set(mustId,{...item,source_refs:refs||[]})}const pageIds=new Set,pageSources=new Map,mustPages=new Map([...mustById.keys()].map(id=>[id,[]])),resolved=[];let slidesReliableForSession=!0;if(!Array.isArray(spec.slides)||!spec.slides.length)add("slides.nonempty-array-required","slides",null,new WisePPTError("deck-spec.slides 必须是非空数组"));else for(const[offset,slide]of spec.slides.entries()){const label=`slides[${offset+1}]`;if(!slide||typeof slide!="object"||Array.isArray(slide)){add("slide.object-required",label,null,new WisePPTError(`${label} 必须是对象`)),slidesReliableForSession=!1;continue}check("slide.unknown-field",label,slide.page_id,()=>assertKnownPreflightKeys(slide,ALLOWED_SLIDE_FIELDS,label));const pageId=check("slide.page-id",`${label}.page_id`,null,()=>plainString(slide.page_id,`${label}.page_id`)),validPageId=pageId&&/^[a-z][a-z0-9-]*$/.test(pageId);pageId&&!validPageId&&add("slide.page-id",`${label}.page_id`,null,new WisePPTError(`${label}.page_id 必须匹配 ^[a-z][a-z0-9-]*$: ${pageId}`)),validPageId&&pageIds.has(pageId)?add("slide.duplicate-page-id",`${label}.page_id`,pageId,new WisePPTError(`page_id 重复: ${pageId}`)):validPageId&&pageIds.add(pageId);const issuePageId=validPageId?pageId:null,pageRole=check("slide.page-role",`${label}.page_role`,issuePageId,()=>plainString(slide.page_role,`${label}.page_role`)),layoutId=check("slide.layout-id",`${label}.layout_id`,issuePageId,()=>plainString(slide.layout_id,`${label}.layout_id`));check("slide.claim",`${label}.claim`,issuePageId,()=>plainString(slide.claim,`${label}.claim`));const layout=layoutId?index.get(layoutId):null;let routeValid=!!(validPageId&&pageRole&&layout);if(layoutId&&!layout&&add("slide.layout-unregistered",`${label}.layout_id`,issuePageId,new WisePPTError(`${label} 使用未登记 layout_id: ${layoutId}`)),layout&&pageRole)if(layout.page_kind==="relationship"){const relation=check("slide.relation",`${label}.relation_key`,issuePageId,()=>plainString(slide.relation_key,`${label}.relation_key`));(!relation||!(layout.relations||[]).includes(relation))&&(relation&&add("slide.relation",`${label}.relation_key`,issuePageId,new WisePPTError(`${pageId} relation_key=${relation} 不受 ${layoutId} 支持`)),routeValid=!1),(layout.page_roles||[]).includes(pageRole)||(add("slide.page-role",`${label}.page_role`,issuePageId,new WisePPTError(`${pageId} page_role=${pageRole} 不受 ${layoutId} 支持`)),routeValid=!1)}else[void 0,null,""].includes(slide.relation_key)||(add("slide.relation",`${label}.relation_key`,issuePageId,new WisePPTError(`${pageId} 是非关系页，不得声明 relation_key`)),routeValid=!1),pageRole!==layout.page_role&&(add("slide.page-role",`${label}.page_role`,issuePageId,new WisePPTError(`${pageId} page_role 必须是 ${layout.page_role}`)),routeValid=!1);slide.emphasis!==void 0&&layout&&check("slide.emphasis",`${label}.emphasis`,issuePageId,()=>{if(!slide.emphasis||typeof slide.emphasis!="object"||Array.isArray(slide.emphasis))throw new WisePPTError(`${pageId}.emphasis 必须是对象`);assertKnownPreflightKeys(slide.emphasis,ALLOWED_EMPHASIS_FIELDS,`${pageId}.emphasis`);const target=plainString(slide.emphasis.target,`${pageId}.emphasis.target`);if(plainString(slide.emphasis.reason,`${pageId}.emphasis.reason`),!new Set((layout.emphasis?.targets||[]).map(item=>item.target_id)).has(target))throw new WisePPTError(`${pageId}.emphasis.target=${target} 不在 ${layoutId} 已审核焦点对象中`)});const refs=check("slide.source-refs",`${label}.source_refs`,issuePageId,()=>referenceList(slide.source_refs,`${pageId}.source_refs`,sourceIds,sourceBacked));refs?(refs.forEach(id=>usedSourceIds.add(id)),check("slide.source-evidence",`${label}.source_evidence`,issuePageId,()=>sourceEvidenceMap(slide.source_evidence,refs,`${pageId}.source_evidence`)),validPageId&&pageSources.set(pageId,refs)):referencesReliable=!1;const mustRefs=check("slide.must-refs",`${label}.must_refs`,issuePageId,()=>referenceList(slide.must_refs,`${pageId}.must_refs`,new Set(mustById.keys())));mustRefs&&mustRefs.forEach(id=>mustPages.get(id).push(pageId));const payload=slide.payload;let payloadValidForSession=!0;if(!payload||typeof payload!="object"||Array.isArray(payload)||!Object.keys(payload).length)add("slide.payload",`${label}.payload`,issuePageId,new WisePPTError(`${pageId}.payload 必须是非空对象`)),payloadValidForSession=!1;else if(layout){const unknownTypes=Object.keys(payload).filter(key=>!(key in PAYLOAD_CATEGORIES));unknownTypes.length&&(add("slide.payload-type",`${label}.payload`,issuePageId,new WisePPTError(`${pageId}.payload 未登记类型: ${unknownTypes.sort().join(", ")}`)),payloadValidForSession=!1),check("slide.payload-exclusive",`${label}.payload`,issuePageId,()=>validatePayloadExclusivity(payload,pageId));const slotMap=new Map((layout.slots||[]).map(slot=>[slot.slot_id,slot])),populated=new Set;let hasPayload=!1;for(const[category,payloadType]of Object.entries(PAYLOAD_CATEGORIES)){const values=payload[category]||{};if(!values||typeof values!="object"||Array.isArray(values)){add("slide.payload-category",`${label}.payload.${category}`,issuePageId,new WisePPTError(`${pageId}.payload.${category} 必须是 slot_id → payload 的对象`)),payloadValidForSession=!1;continue}category in payload&&!Object.keys(values).length&&add("slide.payload-empty-category",`${label}.payload.${category}`,issuePageId,new WisePPTError(`${pageId}.payload.${category} 不得是空对象`));for(const[slotId,slotValue]of Object.entries(values)){const slot=slotMap.get(slotId),itemPath=`${label}.payload.${category}.${slotId}`;if(!slot){add("slide.payload-slot",itemPath,issuePageId,new WisePPTError(`${pageId} payload 使用未登记 slot_id: ${slotId}`)),payloadValidForSession=!1;continue}const shape=check("slide.payload-shape",itemPath,issuePageId,()=>(validatePayloadShape(category,slotValue,`${pageId}.payload.${category}.${slotId}`,slot),!0),!1);payloadSemanticValue(slotValue)||add("slide.payload-semantic",itemPath,issuePageId,new WisePPTError(`${pageId}.payload.${category}.${slotId} 必须有真实非空语义`)),(slot.allowed_payload_types||[]).includes(payloadType)||(add("slide.payload-type",itemPath,issuePageId,new WisePPTError(`${pageId}/${slotId} 不允许 payload 类型 ${payloadType}`)),payloadValidForSession=!1),shape?(populated.add(slotId),hasPayload=!0):payloadValidForSession=!1}}hasPayload||add("slide.payload-empty",`${label}.payload`,issuePageId,new WisePPTError(`${pageId}.payload 至少要有一个登记值具备真实非空语义`));const required=new Set((layout.slots||[]).filter(slot=>slot.required).map(slot=>slot.slot_id)),missing=setDifference(required,populated);missing.length&&add("slide.payload-required",`${label}.payload`,issuePageId,new WisePPTError(`${pageId} 缺少必填 payload slot: ${missing.sort().join(", ")}`))}routeValid&&payloadValidForSession&&layout?resolved.push({slide,layout,slideOffset:offset}):slidesReliableForSession=!1}if(Array.isArray(spec.slides))for(const[mustId,item]of mustById.entries()){const pages=mustPages.get(mustId)||[];if(item.status==="placed"){pageIds.has(item.page_id)?(pages.length!==1||pages[0]!==item.page_id)&&add("must.reference-mismatch","must",null,new WisePPTError(`must ${mustId} 必须只由 page_id=${item.page_id} 的 slide.must_refs 精确引用`)):add("must.page-missing","must",null,new WisePPTError(`must ${mustId} 落到不存在页面: ${item.page_id}`));const pageRefs=pageSources.get(item.page_id);if(pageRefs){const missing=(item.source_refs||[]).filter(source=>!pageRefs.includes(source));missing.length&&add("must.source-mismatch","must",null,new WisePPTError(`must ${mustId} 的来源未登记到落点页 ${item.page_id}: ${missing.sort().join(", ")}`))}}else item.status==="omitted"&&pages.length&&add("must.omitted-referenced","must",null,new WisePPTError(`omitted must ${mustId} 不得被 slide.must_refs 引用`))}if(referencesReliable){const unused=setDifference(sourceIds,usedSourceIds);unused.length&&add("sources.unused","sources",null,new WisePPTError(`sources 含未被使用的来源: ${unused.sort().join(", ")}`))}layoutContext&&slidesReliableForSession&&resolved.length===spec.slides.length&&collectLayoutSessionIssues(resolved,index,layoutContext,collector);try{await validateSpec(root,spec,index)}catch(error){if(!(error instanceof WisePPTError))throw error;collector.errors.some(issue=>issue.message===error.message)||add("spec.validation","$",null,error)}return{status:collector.errors.length?"fail":"pass",error_count:collector.errors.length,errors:collector.errors}}async function validateSpec(root,spec,layoutIndex=null){if(assertKnownKeys(spec,ALLOWED_TOP_LEVEL,"deck-spec 顶层"),spec.contract!==DECK_CONTRACT)throw new WisePPTError(`deck-spec.contract 必须是 ${DECK_CONTRACT}`);if((spec.mode||"standard")!=="standard")throw new WisePPTError("公开 build 只接受 mode=standard；实验骨架不能混入标准成品");walkForbidden(spec);const deck=spec.deck;if(!deck||typeof deck!="object"||Array.isArray(deck))throw new WisePPTError("deck-spec.deck 必须是对象");assertKnownKeys(deck,ALLOWED_DECK_FIELDS,"deck-spec.deck"),plainString(deck.title,"deck.title"),plainString(deck.thesis,"deck.thesis");const inputType=plainString(deck.input_type,"deck.input_type");if(!INPUT_TYPES.has(inputType))throw new WisePPTError(`deck.input_type 未登记: ${inputType}`);if(deck.typography_mode!==void 0&&plainString(deck.typography_mode,"deck.typography_mode"),deck.lang!==void 0&&!/^[A-Za-z]{2,8}(?:-[A-Za-z0-9]{1,8})*$/.test(plainString(deck.lang,"deck.lang")))throw new WisePPTError("deck.lang 必须是受限 ASCII language tag");if(deck.signature!==void 0&&!plainString(deck.signature,"deck.signature").trim())throw new WisePPTError("deck.signature 必须是非空白署名");await resolvedAppearance(root,deck);const{index}=layoutIndex?{index:layoutIndex}:await registryState(root),layoutContext=normalizeLayoutContext(spec.layout_context,new Set(index.keys()));if(!Array.isArray(spec.sources))throw new WisePPTError("deck-spec.sources 必须是数组");if(SOURCE_BACKED_INPUT_TYPES.has(inputType)&&!spec.sources.length)throw new WisePPTError(`deck.input_type=${inputType} 必须登记至少一个 source`);const sourceIds=new Set,usedSourceIds=new Set;if(spec.sources.forEach((source,offset)=>{if(!source||typeof source!="object"||Array.isArray(source))throw new WisePPTError(`sources[${offset+1}] 必须是对象`);assertKnownKeys(source,ALLOWED_SOURCE_FIELDS,`sources[${offset+1}]`);const sourceId=plainString(source.source_id,`sources[${offset+1}].source_id`);if(plainString(source.title,`sources[${offset+1}].title`),sourceIds.has(sourceId))throw new WisePPTError(`source_id 重复: ${sourceId}`);sourceIds.add(sourceId)}),!Array.isArray(spec.must))throw new WisePPTError("deck-spec.must 必须是数组");const mustById=new Map;for(const[offset,item]of spec.must.entries()){const label=`must[${offset+1}]`;if(!item||typeof item!="object"||Array.isArray(item))throw new WisePPTError(`${label} 必须是对象`);assertKnownKeys(item,ALLOWED_MUST_FIELDS,label);const mustId=plainString(item.must_id,`${label}.must_id`);if(mustById.has(mustId))throw new WisePPTError(`must_id 重复: ${mustId}`);if(plainString(item.content,`${label}.content`),!["placed","omitted"].includes(plainString(item.status,`${label}.status`)))throw new WisePPTError(`${label}.status 只能是 placed 或 omitted`);if(referenceList(item.source_refs,`${label}.source_refs`,sourceIds,SOURCE_BACKED_INPUT_TYPES.has(inputType)).forEach(id=>usedSourceIds.add(id)),item.status==="placed"){if(plainString(item.page_id,`${label}.page_id`),plainString(item.visible_evidence,`${label}.visible_evidence`),![void 0,null,""].includes(item.reason))throw new WisePPTError(`${label} 已落页，不得声明 omitted reason`)}else if(plainString(item.reason,`${label}.reason`),![void 0,null,""].includes(item.page_id)||![void 0,null,""].includes(item.visible_evidence))throw new WisePPTError(`${label} 已省略，不得声明 page_id/visible_evidence`);mustById.set(mustId,item)}if(!Array.isArray(spec.slides)||!spec.slides.length)throw new WisePPTError("deck-spec.slides 必须是非空数组");const pageIds=new Set,mustPages=new Map([...mustById.keys()].map(id=>[id,[]])),pageSources=new Map,resolved=[];for(const[offset,slide]of spec.slides.entries()){const label=`slides[${offset+1}]`;if(!slide||typeof slide!="object"||Array.isArray(slide))throw new WisePPTError(`${label} 必须是对象`);assertKnownKeys(slide,ALLOWED_SLIDE_FIELDS,label);const pageId=plainString(slide.page_id,`${label}.page_id`);if(!/^[a-z][a-z0-9-]*$/.test(pageId))throw new WisePPTError(`${label}.page_id 必须匹配 ^[a-z][a-z0-9-]*$: ${pageId}`);if(pageIds.has(pageId))throw new WisePPTError(`page_id 重复: ${pageId}`);pageIds.add(pageId);const pageRole=plainString(slide.page_role,`${label}.page_role`),layoutId=plainString(slide.layout_id,`${label}.layout_id`);plainString(slide.claim,`${label}.claim`);const layout=index.get(layoutId);if(!layout)throw new WisePPTError(`${label} 使用未登记 layout_id: ${layoutId}`);if(layout.page_kind==="relationship"){const relation=plainString(slide.relation_key,`${label}.relation_key`);if(!(layout.relations||[]).includes(relation))throw new WisePPTError(`${pageId} relation_key=${relation} 不受 ${layoutId} 支持`);if(!(layout.page_roles||[]).includes(pageRole))throw new WisePPTError(`${pageId} page_role=${pageRole} 不受 ${layoutId} 支持`)}else{if(![void 0,null,""].includes(slide.relation_key))throw new WisePPTError(`${pageId} 是非关系页，不得声明 relation_key`);if(pageRole!==layout.page_role)throw new WisePPTError(`${pageId} page_role 必须是 ${layout.page_role}`)}if(slide.emphasis!==void 0){if(!slide.emphasis||typeof slide.emphasis!="object"||Array.isArray(slide.emphasis))throw new WisePPTError(`${pageId}.emphasis 必须是对象`);assertKnownKeys(slide.emphasis,ALLOWED_EMPHASIS_FIELDS,`${pageId}.emphasis`);const target=plainString(slide.emphasis.target,`${pageId}.emphasis.target`);if(plainString(slide.emphasis.reason,`${pageId}.emphasis.reason`),!new Set((layout.emphasis?.targets||[]).map(item=>item.target_id)).has(target))throw new WisePPTError(`${pageId}.emphasis.target=${target} 不在 ${layoutId} 已审核焦点对象中`)}const refs=referenceList(slide.source_refs,`${pageId}.source_refs`,sourceIds,SOURCE_BACKED_INPUT_TYPES.has(inputType));refs.forEach(id=>usedSourceIds.add(id)),sourceEvidenceMap(slide.source_evidence,refs,`${pageId}.source_evidence`),pageSources.set(pageId,refs),referenceList(slide.must_refs,`${pageId}.must_refs`,new Set(mustById.keys())).forEach(id=>mustPages.get(id).push(pageId));const payload=slide.payload;if(!payload||typeof payload!="object"||Array.isArray(payload)||!Object.keys(payload).length)throw new WisePPTError(`${pageId}.payload 必须是非空对象`);const unknownTypes=Object.keys(payload).filter(key=>!(key in PAYLOAD_CATEGORIES));if(unknownTypes.length)throw new WisePPTError(`${pageId}.payload 未登记类型: ${unknownTypes.sort().join(", ")}`);validatePayloadExclusivity(payload,pageId);const slotMap=new Map((layout.slots||[]).map(slot=>[slot.slot_id,slot])),populated=new Set;let hasPayload=!1;for(const[category,payloadType]of Object.entries(PAYLOAD_CATEGORIES)){const values=payload[category]||{};if(!values||typeof values!="object"||Array.isArray(values))throw new WisePPTError(`${pageId}.payload.${category} 必须是 slot_id → payload 的对象`);if(category in payload&&!Object.keys(values).length)throw new WisePPTError(`${pageId}.payload.${category} 不得是空对象`);for(const[slotId,slotValue]of Object.entries(values)){const slot=slotMap.get(slotId);if(!slot)throw new WisePPTError(`${pageId} payload 使用未登记 slot_id: ${slotId}`);const itemLabel=`${pageId}.payload.${category}.${slotId}`;if(validatePayloadShape(category,slotValue,itemLabel,slot),!payloadSemanticValue(slotValue))throw new WisePPTError(`${itemLabel} 必须有真实非空语义`);if(!(slot.allowed_payload_types||[]).includes(payloadType))throw new WisePPTError(`${pageId}/${slotId} 不允许 payload 类型 ${payloadType}`);populated.add(slotId),hasPayload=!0}}if(!hasPayload)throw new WisePPTError(`${pageId}.payload 至少要有一个登记值具备真实非空语义`);const required=new Set((layout.slots||[]).filter(slot=>slot.required).map(slot=>slot.slot_id)),missing=setDifference(required,populated);if(missing.length)throw new WisePPTError(`${pageId} 缺少必填 payload slot: ${missing.sort().join(", ")}`);resolved.push({slide,layout})}for(const[mustId,item]of mustById.entries()){const pages=mustPages.get(mustId);if(item.status==="placed"){if(!pageIds.has(item.page_id))throw new WisePPTError(`must ${mustId} 落到不存在页面: ${item.page_id}`);if(pages.length!==1||pages[0]!==item.page_id)throw new WisePPTError(`must ${mustId} 必须只由 page_id=${item.page_id} 的 slide.must_refs 精确引用`);const missing=item.source_refs.filter(source=>!pageSources.get(item.page_id).includes(source));if(missing.length)throw new WisePPTError(`must ${mustId} 的来源未登记到落点页 ${item.page_id}: ${missing.sort().join(", ")}`)}else if(pages.length)throw new WisePPTError(`omitted must ${mustId} 不得被 slide.must_refs 引用`)}const unused=setDifference(sourceIds,usedSourceIds);if(unused.length)throw new WisePPTError(`sources 含未被使用的来源: ${unused.sort().join(", ")}`);return Object.defineProperty(resolved,"layoutSession",{value:buildLayoutSessionReceipt(resolved,index,layoutContext),enumerable:!1}),resolved}function queryLayouts(registry,filters){const required=new Set(filters.requires||[]);if([...required].some(item=>!["text","data","icon"].includes(item)))throw new WisePPTError("查询含未登记 payload 类型");if(filters.pageKind==="nonrelationship"&&filters.relationKey)throw new WisePPTError("非关系页查询不得传 --relation-key");if(filters.contentItems!==void 0&&(!Number.isSafeInteger(filters.contentItems)||filters.contentItems<0))throw new WisePPTError("contentItems 必须是非负安全整数");const detailOnly=!!filters.layoutId;if(detailOnly&&(filters.selectionSeed!==void 0||(filters.layoutUsage||[]).length))throw new WisePPTError("单骨架详情查询不得传 selection seed 或 layout usage");const selectionSeed=detailOnly?null:normalizeSelectionSeed(filters.selectionSeed,"layout query selection_seed"),normalizedUsage=normalizeLayoutUsage(registry,filters.layoutUsage||[]),currentUsage=usageMap(normalizedUsage),order=registryIndex(registry),ranked=(registry.layouts||[]).filter(layout=>{if(filters.layoutId&&layout.layout_id!==filters.layoutId||filters.pageKind&&layout.page_kind!==filters.pageKind)return!1;const roles=layout.page_kind==="relationship"?layout.page_roles:[layout.page_role];return!(filters.pageRole&&!(roles||[]).includes(filters.pageRole)||filters.relationKey&&!(layout.relations||[]).includes(filters.relationKey)||[...required].some(type=>!(layout.allowed_payload_types||[]).includes(type)))}).map(layout=>({layout_id:layout.layout_id,display_code:layout.display_code,page_kind:layout.page_kind,name:layout.name||layout.display_code,description:layout.description||"",structure_summary:layout.structure?.summary||"",leaf_count:layout.structure?.leaf_count,page_role:layout.page_role,page_roles:layout.page_roles||[],relations:layout.relations||[],reading_order:layout.reading_order||[],capacity:layout.capacity||{},allowed_payload_types:layout.allowed_payload_types||[],claim_binding:layout.claim_binding,derivation:layout.derivation,emphasis:structuredClone(layout.emphasis||{access:"none",targets:[]}),icon_slots:structuredClone(layout.icon_slots||[]),slots:(layout.slots||[]).map(slot=>Object.fromEntries(["slot_id","purpose","required","visual_role","capacity","allowed_payload_types","payload_schema"].filter(key=>key in slot).map(key=>[key,slot[key]]))),usage_count:usageFor(currentUsage,layout.layout_id).count,last_sequence:usageFor(currentUsage,layout.layout_id).last_sequence})).sort((left,right)=>detailOnly?order.get(left.layout_id)-order.get(right.layout_id):compareByUsage(left,right,currentUsage,selectionSeed,order)).map((layout,offset)=>({...layout,selection_rank:detailOnly?null:offset+1,requires_override_if_selected:detailOnly?null:offset>0}));return filters.contentItems===void 0?ranked:ranked.filter(layout=>(layout.slots||[]).some(slot=>Number.isInteger(slot.capacity?.min_items)&&Number.isInteger(slot.capacity?.max_items)&&slot.capacity.min_items<=filters.contentItems&&filters.contentItems<=slot.capacity.max_items&&(slot.allowed_payload_types||[]).length))}function layoutAgentDefinition(layout){return{layout_id:layout.layout_id,display_code:layout.display_code,page_kind:layout.page_kind,page_role:layout.page_role,page_roles:structuredClone(layout.page_roles||[]),relations:structuredClone(layout.relations||[]),allowed_payload_types:structuredClone(layout.allowed_payload_types||[]),name:layout.name||layout.display_code,description:layout.description||"",structure_summary:layout.structure_summary||"",leaf_count:layout.leaf_count,reading_order:structuredClone(layout.reading_order||[]),capacity:structuredClone(layout.capacity||{}),claim_binding:structuredClone(layout.claim_binding??null),emphasis:structuredClone(layout.emphasis||{access:"none",targets:[]}),icon_slots:structuredClone(layout.icon_slots||[]),slots:structuredClone(layout.slots||[])}}function layoutPlanIssue(code,pageId,pathName,message){return{code,page_id:pageId,path:pathName,message}}function planLayouts(registry,request,options={}){if(assertKnownKeys(request,ALLOWED_LAYOUT_PLAN_FIELDS,"layout plan 顶层"),request.format!==LAYOUT_PLAN_REQUEST_FORMAT)throw new WisePPTError(`layout plan format 必须是 ${LAYOUT_PLAN_REQUEST_FORMAT}`);if(!Array.isArray(request.pages)||!request.pages.length)throw new WisePPTError("layout plan pages 必须是非空数组");const index=new Map((registry.layouts||[]).map(layout=>[layout.layout_id,layout])),layoutIds=new Set(index.keys()),hasContext=request.layout_context!==void 0,hasNewSeed=options.newSelectionSeed!==void 0;if(hasContext===hasNewSeed)throw new WisePPTError("layout plan 必须且只能在 request.layout_context 与 --new-session 中选择一个");const layoutContext=hasContext?normalizeLayoutContext(request.layout_context,layoutIds):{scope:"session",selection_seed:normalizeSelectionSeed(options.newSelectionSeed,"layout plan new selection_seed"),prior_total:0,usage:[]};if(!Number.isSafeInteger(layoutContext.prior_total+request.pages.length))throw new WisePPTError("layout plan post_total 超过安全整数范围");const selectedCount=request.pages.filter(page=>page&&typeof page=="object"&&!Array.isArray(page)&&Object.hasOwn(page,"selected_layout_id")).length;if(selectedCount!==0&&selectedCount!==request.pages.length)throw new WisePPTError("layout plan selected_layout_id 必须整副全部填写或全部省略");const mode=selectedCount?"resolved":"proposal",currentUsage=usageMap(layoutContext),definitions=new Map,pageIds=new Set,pages=[],errors=[];for(const[offset,page]of request.pages.entries()){const label=`pages[${offset+1}]`;if(!page||typeof page!="object"||Array.isArray(page))throw new WisePPTError(`${label} 必须是对象`);assertKnownKeys(page,ALLOWED_LAYOUT_PLAN_PAGE_FIELDS,label);const pageId=plainString(page.page_id,`${label}.page_id`);if(!/^[a-z][a-z0-9-]*$/.test(pageId))throw new WisePPTError(`${label}.page_id 必须匹配 ^[a-z][a-z0-9-]*$: ${pageId}`);if(pageIds.has(pageId))throw new WisePPTError(`layout plan page_id 重复: ${pageId}`);pageIds.add(pageId);const pageKind=plainString(page.page_kind,`${label}.page_kind`);if(!["relationship","nonrelationship"].includes(pageKind))throw new WisePPTError(`${label}.page_kind 只能是 relationship 或 nonrelationship`);const pageRole=plainString(page.page_role,`${label}.page_role`),relationKey=page.relation_key===void 0||page.relation_key===null?null:plainString(page.relation_key,`${label}.relation_key`);if(pageKind==="relationship"&&!relationKey)throw new WisePPTError(`${label}.relation_key 对关系页必须非空`);if(pageKind==="nonrelationship"&&relationKey)throw new WisePPTError(`${label} 是非关系页，不得声明 relation_key`);const requires=page.requires===void 0?[]:page.requires;if(!Array.isArray(requires)||requires.some(item=>!["text","data","icon"].includes(item))||new Set(requires).size!==requires.length)throw new WisePPTError(`${label}.requires 必须是 text/data/icon 组成的无重复数组`);const contentItems=page.content_items===void 0?void 0:nonnegativeInteger(page.content_items,`${label}.content_items`);if(mode==="proposal"&&page.layout_override!==void 0)throw new WisePPTError(`${label}.layout_override 只能在整副填写 selected_layout_id 后使用`);const currentUsageEntries=[...currentUsage.values()].sort((left,right)=>compareAscii(left.layout_id,right.layout_id)),filters={pageKind,pageRole,relationKey:relationKey??void 0,requires,contentItems,layoutUsage:currentUsageEntries,selectionSeed:layoutContext.selection_seed},matches=queryLayouts(registry,filters),rankingPool=contentItems===void 0?matches:queryLayouts(registry,{...filters,contentItems:void 0}),selectedLayoutId=mode==="resolved"?plainString(page.selected_layout_id,`${label}.selected_layout_id`):matches[0]?.layout_id??null,selected=matches.find(layout=>layout.layout_id===selectedLayoutId);if(mode==="proposal")for(const layout of matches)definitions.has(layout.layout_id)||definitions.set(layout.layout_id,layoutAgentDefinition(layout));if(!matches.length)errors.push(layoutPlanIssue("layout.no-candidate",pageId,label,`${pageId} 没有符合页型、关系、payload 与容量条件的登记骨架`));else if(!selected){const hardCandidate=rankingPool.some(layout=>layout.layout_id===selectedLayoutId);errors.push(layoutPlanIssue(hardCandidate?"layout.selected-capacity-mismatch":"layout.selected-not-candidate",pageId,`${label}.selected_layout_id`,hardCandidate?`${pageId} 选择的 ${selectedLayoutId} 不接受 content_items=${contentItems}`:`${pageId} 选择的 ${selectedLayoutId} 不符合页型、关系或 payload 硬条件`))}const selectionRank=selected?.selection_rank??null,requiresOverride=Number.isInteger(selectionRank)&&selectionRank>1;let layoutOverride=null;if(selected&&mode==="resolved")try{layoutOverride=normalizedLayoutOverride({page_id:pageId,layout_id:selected.layout_id,layout_override:page.layout_override},requiresOverride,selectionRank,rankingPool[0]?.layout_id??null)}catch(error){if(!(error instanceof WisePPTError))throw error;errors.push(layoutPlanIssue("layout.override-invalid",pageId,`${label}.layout_override`,error.message))}if(pages.push({page_id:pageId,page_kind:pageKind,page_role:pageRole,relation_key:relationKey,requires:structuredClone(requires),content_items:contentItems??null,selection_state:matches.length?layoutSelectionState(rankingPool):"no-candidate",preferred_layout_id:rankingPool[0]?.layout_id??null,suggested_layout_id:matches[0]?.layout_id??null,selected_layout_id:selected?.layout_id??selectedLayoutId,selection_rank:selectionRank,requires_override_if_selected:requiresOverride,suggested_override_basis:mode==="proposal"&&requiresOverride&&contentItems!==void 0?"capacity":null,layout_override:layoutOverride,candidates:matches.map(layout=>({layout_id:layout.layout_id,usage_count:layout.usage_count,last_sequence:layout.last_sequence,selection_rank:layout.selection_rank,requires_override_if_selected:layout.requires_override_if_selected}))}),selected){const selectedBefore=usageFor(currentUsage,selected.layout_id);currentUsage.set(selected.layout_id,{layout_id:selected.layout_id,count:selectedBefore.count+1,last_sequence:layoutContext.prior_total+offset+1})}}const order=registryIndex(registry),layoutDefinitions=[...definitions.values()].sort((left,right)=>order.get(left.layout_id)-order.get(right.layout_id)),projectedPostUsage=[...currentUsage.values()].sort((left,right)=>compareAscii(left.layout_id,right.layout_id));return{status:errors.length?"fail":"pass",mode,registry_count:registry.layouts.length,layout_context:structuredClone(layoutContext),projection:{valid:errors.length===0,basis:mode==="proposal"?"first-compatible-candidate":"explicit-selected-layouts",post_total:errors.length?null:layoutContext.prior_total+request.pages.length,post_usage:errors.length?[]:projectedPostUsage},page_count:pages.length,pages,layout_definition_policy:mode==="proposal"?"all-candidates-once":"reuse-proposal-brief",layout_definitions:layoutDefinitions,error_count:errors.length,errors}}async function loadSeed(root,displayCode){const seed=await readJson(path.join(root,"capabilities/layouts/seeds",`${displayCode.toUpperCase()}.json`),`骨架 ${displayCode} seed`);if(seed.contract!=="wise-ppt-layout-seed@4")throw new WisePPTError(`骨架 ${displayCode} seed 合同错误`);if(!/^[0-9a-f]{64}$/.test(String(seed.seed_sha256||"")))throw new WisePPTError(`骨架 ${displayCode} seed 缺少合法哈希`);return seed}function elementAttributes(node,idMap){const result={},projectedSurface=node.attribs?.["data-theme-binding-projected"]==="surface",materializedIcon="data-vnext-icon-key"in(node.attribs||{})&&"data-theme-icon-verified"in(node.attribs||{});for(const originalName of Object.keys(node.attribs||{}).sort()){const name=originalName.toLowerCase();if(name==="data-vnext-payload-value"||name==="data-theme-type-role"||name==="data-theme-language"||["data-theme-binding-source","data-theme-binding-id","data-theme-binding-projected"].includes(name)||["data-theme-identity-source","data-theme-identity-group","data-theme-identity-treatment"].includes(name)||projectedSurface&&["data-theme-surface","data-theme-layer"].includes(name)||"data-vnext-icon-key"in(node.attribs||{})&&name==="data-theme-icon-verified"||materializedIcon&&ICON_SOURCE_PRESENTATION_ATTRIBUTES.includes(name))continue;let value=String(node.attribs[originalName]);if(materializedIcon&&name==="viewbox"&&(value="0 0 64 64"),name==="id"&&idMap.has(value))value=idMap.get(value);else for(const[old,replacement]of idMap.entries())value=value.replaceAll(`url(#${old})`,`url(#${replacement})`),value===`#${old}`&&(value=`#${replacement}`);"data-vnext-icon-key"in(node.attribs||{})&&["data-icon","data-icon-source"].includes(name)&&(value="__PAYLOAD_ICON__"),result[name]=value}return result}function rewriteMemberSelector(selector,idMap){let result=String(selector);for(const[old,replacement]of[...idMap.entries()].sort((a,b)=>b[0].length-a[0].length))result=result.replace(new RegExp(`#${old.replace(/[.*+?^${}()|[\]\\]/g,"\\$&")}(?![A-Za-z0-9_-])`,"g"),`#${replacement}`);return result}function selectExactThemeBinding(stage,rawSelector,idMap,binding,label){const selector=rewriteMemberSelector(rawSelector,idMap);let matched;try{matched=stage.find(selector).addBack(selector)}catch(error){throw new WisePPTError(`${label}.${binding.binding_id} 选择器非法: ${selector}: ${error.message}`)}if(matched.length!==binding.expected_count)throw new WisePPTError(`${label}.${binding.binding_id} 必须精确命中 ${binding.expected_count} 个节点，实际 ${matched.length}`);return matched}function projectLayoutThemeBindings(stage,idMap,entry,label){if(!entry)return;const claimed=new Set,claimNodes=(matched,binding)=>matched.each((_index,node)=>{if(claimed.has(node))throw new WisePPTError(`${label}.${binding.binding_id} 与另一个主题绑定命中同一节点`);claimed.add(node);const carrier=stage._make?stage._make(node):null,attributes=node.attribs||{};if(attributes["data-theme-binding-source"]||attributes["data-theme-binding-id"])throw new WisePPTError(`${label}.${binding.binding_id} 命中了预置主题绑定标记`);carrier?carrier.attr("data-theme-binding-source",LAYOUT_THEME_BINDING_SOURCE).attr("data-theme-binding-id",binding.binding_id):(attributes["data-theme-binding-source"]=LAYOUT_THEME_BINDING_SOURCE,attributes["data-theme-binding-id"]=binding.binding_id)});for(const binding of entry.surface_bindings||[]){const matched=selectExactThemeBinding(stage,binding.selector,idMap,binding,label);claimNodes(matched,binding),matched.each((_index,node)=>{const carrier=stage._make?stage._make(node):null,attrs=node.attribs||{},existingSurface=attrs["data-theme-surface"],existingLayer=attrs["data-theme-layer"];if(existingSurface&&existingSurface!==binding.surface)throw new WisePPTError(`${label}.${binding.binding_id} 与 seed 的 surface 语义冲突`);if(existingLayer&&existingLayer!==binding.layer)throw new WisePPTError(`${label}.${binding.binding_id} 与 seed 的 layer 语义冲突`);const changed=existingSurface!==binding.surface||binding.layer!==null&&existingLayer!==binding.layer;carrier?(carrier.attr("data-theme-surface",binding.surface),binding.layer!==null&&carrier.attr("data-theme-layer",binding.layer),changed&&carrier.attr("data-theme-binding-projected","surface")):(attrs["data-theme-surface"]=binding.surface,binding.layer!==null&&(attrs["data-theme-layer"]=binding.layer),changed&&(attrs["data-theme-binding-projected"]="surface"))})}for(const binding of entry.type_bindings||[]){const matched=selectExactThemeBinding(stage,binding.selector,idMap,binding,label);claimNodes(matched,binding),matched.attr("data-theme-type-role",binding.role)}const identityClaimed=new Set;for(const group of entry.identity_groups||[])for(const[memberOffset,member]of group.members.entries()){const identityBinding={...member,binding_id:`${group.group_id}.member-${memberOffset+1}`};selectExactThemeBinding(stage,member.selector,idMap,identityBinding,label).each((_index,node)=>{if(identityClaimed.has(node))throw new WisePPTError(`${label}.${group.group_id} 与另一个 identity member 命中同一节点`);identityClaimed.add(node);const attrs=node.attribs||{};if(attrs["data-theme-identity-source"]||attrs["data-theme-identity-group"]||attrs["data-theme-identity-treatment"])throw new WisePPTError(`${label}.${group.group_id} 命中了预置主题身份标记`);const carrier=stage._make?stage._make(node):null;carrier?carrier.attr("data-theme-identity-source",LAYOUT_THEME_IDENTITY_SOURCE).attr("data-theme-identity-group",group.group_id).attr("data-theme-identity-treatment",member.treatment):(attrs["data-theme-identity-source"]=LAYOUT_THEME_IDENTITY_SOURCE,attrs["data-theme-identity-group"]=group.group_id,attrs["data-theme-identity-treatment"]=member.treatment)})}}function validateProjectedLayoutThemeBindings(stage,entry,label){const marked=stage.find("[data-theme-binding-source], [data-theme-binding-id], [data-theme-binding-projected]").addBack("[data-theme-binding-source], [data-theme-binding-id], [data-theme-binding-projected]");marked.each((_index,node)=>{const attrs=node.attribs||{};if(attrs["data-theme-binding-source"]!==LAYOUT_THEME_BINDING_SOURCE||!attrs["data-theme-binding-id"]||attrs["data-theme-binding-projected"]!==void 0&&attrs["data-theme-binding-projected"]!=="surface")throw new WisePPTError(`${label} 含不完整或伪造的逐版式主题绑定标记`)});const all=stage.find(`[data-theme-binding-source="${LAYOUT_THEME_BINDING_SOURCE}"]`).addBack(`[data-theme-binding-source="${LAYOUT_THEME_BINDING_SOURCE}"]`),expected=[...entry?.surface_bindings||[],...entry?.type_bindings||[]];if(!entry){if(marked.length)throw new WisePPTError(`${label} 不应含逐版式主题绑定标记`);return}const expectedIds=new Set(expected.map(binding=>binding.binding_id));all.each((_index,node)=>{const bindingId=node.attribs?.["data-theme-binding-id"];if(!expectedIds.has(bindingId))throw new WisePPTError(`${label} 含未知主题绑定标记: ${bindingId||"-"}`)});for(const binding of entry.surface_bindings||[]){const matched=all.filter(`[data-theme-binding-id="${binding.binding_id}"]`);if(matched.length!==binding.expected_count)throw new WisePPTError(`${label}.${binding.binding_id} 投影数量漂移`);matched.each((_index,node)=>{if(node.attribs?.["data-theme-surface"]!==binding.surface||binding.layer!==null&&node.attribs?.["data-theme-layer"]!==binding.layer)throw new WisePPTError(`${label}.${binding.binding_id} 材料语义投影漂移`)})}for(const binding of entry.type_bindings||[]){const matched=all.filter(`[data-theme-binding-id="${binding.binding_id}"]`);if(matched.length!==binding.expected_count||matched.filter(`[data-theme-type-role="${binding.role}"]`).length!==binding.expected_count)throw new WisePPTError(`${label}.${binding.binding_id} 字体角色投影漂移`)}const identityMarked=stage.find("[data-theme-identity-source], [data-theme-identity-group], [data-theme-identity-treatment]").addBack("[data-theme-identity-source], [data-theme-identity-group], [data-theme-identity-treatment]");if(!entry){if(identityMarked.length)throw new WisePPTError(`${label} 不应含逐版式主题身份标记`);return}const identityGroups=new Map((entry.identity_groups||[]).map(group=>[group.group_id,group]));identityMarked.each((_index,node)=>{const attrs=node.attribs||{},group=identityGroups.get(attrs["data-theme-identity-group"]);if(attrs["data-theme-identity-source"]!==LAYOUT_THEME_IDENTITY_SOURCE||!group||!group.members.some(member=>member.treatment===attrs["data-theme-identity-treatment"]))throw new WisePPTError(`${label} 含不完整或伪造的逐版式主题身份标记`)});for(const group of entry.identity_groups||[])for(const member of group.members){const matched=identityMarked.filter(`[data-theme-identity-group="${group.group_id}"][data-theme-identity-treatment="${member.treatment}"]`),expectedForTreatment=group.members.filter(item=>item.treatment===member.treatment).reduce((total,item)=>total+item.expected_count,0);if(matched.length!==expectedForTreatment)throw new WisePPTError(`${label}.${group.group_id}/${member.treatment} 主题身份投影数量漂移`)}}function structureDigest(stageHtml){const stage=load(stageHtml,null,!1)(".stage").first();if(!stage.length)throw new WisePPTError("seed 缺少 .stage");const idMap=new Map;stage.find("[id]").each((index,node)=>{node.attribs?.id&&idMap.set(node.attribs.id,`ID${String(index+1).padStart(4,"0")}`)});function walk(node,insideText=!1){const attrs=node.attribs||{},signatureSurface=attrs["data-template-part"]==="signature",textSurface=insideText||"data-vnext-text-key"in attrs||"data-vnext-claim-key"in attrs||signatureSurface,opaqueIcon="data-vnext-icon-key"in attrs,children=[];if(!opaqueIcon){for(const child of node.children||[])child.type==="tag"||child.type==="script"||child.type==="style"?children.push(walk(child,textSurface)):["text","comment"].includes(child.type)&&child.data.trim()&&!textSurface&&children.push(["#text"]);signatureSurface&&children.push(["#text"])}return[String(node.name).toLowerCase(),elementAttributes(node,idMap),children]}return sha256Text(canonicalJson(walk(stage[0])))}function projectThemeTypeRoles(sectionHtml){const $=load(sectionHtml,null,!1),pageRole=$("section.slide").first().attr("data-page-role"),locked=new Map;$("[data-theme-binding-source][data-theme-type-role]").each((_index,node)=>locked.set(node,$(node).attr("data-theme-type-role")));const assign=(selector,role)=>$(selector).attr("data-theme-type-role",role),isEnglish=value=>{const source=String(value||""),latin=(source.match(/[A-Za-z]/g)||[]).length,han=(source.match(/[\u3400-\u9FFF]/g)||[]).length;return latin>=2&&latin>han};assign('p, [data-text-kind="body"], [data-template-slot-kind="text"]:not([data-template-part="primary"])',"body"),assign('.doc.tl, [data-page-shell="top-left"]',"top-left-kicker"),assign('.folio, [data-page-shell="bottom-left"]',"bottom-left-folio"),assign('.caption, [data-page-shell="bottom-takeaway"]',"bottom-takeaway"),assign('[data-template-part="primary"][data-template-slot-kind="text"], [data-primary-text]',pageRole==="cover"?"cover-title":"zh-content-title"),assign('[data-text-kind="english-title"], [data-theme-language="en"][data-primary-text]',"en-content-title"),assign('strong[data-text-kind="english"], [data-theme-language="en"] strong',"en-bold"),assign('[data-text-kind="label"]',"label"),assign('[data-text-kind="number"], [data-text-kind="metric"]',"number"),assign('[data-text-kind="chart-label"], [data-theme-chart-text]',"chart-text"),$('[data-theme-type-role="zh-content-title"], [data-theme-type-role="cover-title"]').each((_index,node)=>{isEnglish($(node).text())&&$(node).attr("data-theme-type-role","en-content-title").attr("data-theme-language","en")}),$("strong").each((_index,node)=>{isEnglish($(node).text())&&$(node).attr("data-theme-type-role","en-bold").attr("data-theme-language","en")});for(const[node,role]of locked)$(node).attr("data-theme-type-role",role);return $.html().replace(/&#x([0-9a-f]+);/gi,(entity,hex)=>{const codePoint=Number.parseInt(hex,16);return codePoint>=128?String.fromCodePoint(codePoint):entity})}function prefixDomIds($,stage,prefix){const mapping=new Map;return stage.find("[id]").addBack("[id]").each((_index,node)=>{const old=node.attribs?.id;old&&mapping.set(old,`${prefix}${old}`)}),stage.find("*").addBack().each((_index,node)=>{for(const[name,raw]of Object.entries(node.attribs||{})){let value=String(raw);if(name==="id"&&mapping.has(value))value=mapping.get(value);else for(const[old,replacement]of mapping.entries())value=value.replaceAll(`url(#${old})`,`url(#${replacement})`),value===`#${old}`&&(value=`#${replacement}`);node.attribs[name]=value}}),mapping}function safePagePrefix(pageId){return`vnext-${pageId.replace(/[^a-zA-Z0-9_-]+/g,"-").replace(/^-+|-+$/g,"")||"page"}-`}function rewriteScopedCss(css,layoutId,pageId,idMap){let result=css.replace(`.slide[data-layout-id="${layoutId}"]`,`.slide[data-page-id="${pageId}"]`);for(const[old,replacement]of[...idMap.entries()].sort((a,b)=>b[0].length-a[0].length))result=result.replace(new RegExp(`#${old.replace(/[.*+?^${}()|[\]\\]/g,"\\$&")}(?![A-Za-z0-9_-])`,"g"),`#${replacement}`);return result}function materializedEmphasis(slide,layout,stage,idMap){if(!slide.emphasis)return null;const target=(layout.emphasis?.targets||[]).find(item=>item.target_id===slide.emphasis.target);if(!target)throw new WisePPTError(`${slide.page_id}.emphasis.target 未落到已审核焦点对象`);const registeredMembers=layout._emphasis_targets?.get(slide.emphasis.target);if(!registeredMembers?.length)throw new WisePPTError(`${slide.page_id}.emphasis.target 缺少编译器私有成员`);const members=registeredMembers.map(member=>({selector:rewriteMemberSelector(member.selector,idMap),role:member.role,treatment:member.treatment,expected_count:member.expected_count,...member.paint?{paint:member.paint}:{}}));for(const member of members){let matched;try{matched=stage.find(member.selector).addBack(member.selector)}catch(error){throw new WisePPTError(`${slide.page_id} 强调目标选择器非法: ${member.selector}: ${error.message}`)}if(matched.length!==member.expected_count)throw new WisePPTError(`${slide.page_id} 强调目标必须精确命中 ${member.expected_count} 个节点，实际 ${matched.length}: ${member.selector}`)}return{target:slide.emphasis.target,ref:`emphasis.${slide.page_id}.${slide.emphasis.target}`,reason:slide.emphasis.reason,roles:target.member_roles,members}}function payloadFields(raw,bindings,label){const keys=bindings.map(item=>item.key);if(!keys.length)throw new WisePPTError(`${label} 没有登记 binding key`);if(Array.isArray(raw))throw new WisePPTError(`${label} 禁止位置数组`);if(keys.length>1&&(!raw||typeof raw!="object"))throw new WisePPTError(`${label} 有 ${keys.length} 个字段，必须使用 fields/items 控制对象`);if(raw&&typeof raw=="object"){if(Object.keys(raw).length!==2||!("fields"in raw)||!("items"in raw))throw new WisePPTError(`${label} 只能使用 {fields, items}`);if(!raw.fields||typeof raw.fields!="object"||Array.isArray(raw.fields))throw new WisePPTError(`${label}.fields 必须是对象`);if(!Number.isInteger(raw.items)||raw.items<0)throw new WisePPTError(`${label}.items 必须是非负整数`);return validateExactFields(raw.fields,bindings,raw.items,label),{fields:{...raw.fields},items:raw.items}}const fields={[keys[0]]:raw};return validateExactFields(fields,bindings,1,label),{fields,items:1}}function textNodes(node){const result=[];function visit(current){for(const child of current.children||[])child.type==="text"&&child.data.trim()?result.push(child):child.children&&visit(child)}return visit(node),result}function replaceText($,selection,value){const text=String(value),nodes=textNodes(selection[0]);if(!nodes.length){selection.text(text);return}const parts=text.split(`
`);if(parts.length>nodes.length)throw new WisePPTError(`字段 ${selection.attr("data-vnext-text-key")||selection.attr("data-vnext-claim-key")||""} 的换行数超过锁定 DOM 容量`);nodes.forEach((node,index)=>{node.data=parts[index]||""})}function svgBodyDigest(svg){function canonicalNode(node){if(node.type==="text")return node.data.trim()?["#text",node.data]:null;if(node.type==="comment")return["#comment",node.data];if(!node.name)return null;const attributes=Object.fromEntries(Object.entries(node.attribs||{}).map(([name,value])=>[name.toLowerCase(),String(value)]).sort(([left],[right])=>compareAscii(left,right))),children=(node.children||[]).map(canonicalNode).filter(Boolean);return[String(node.name).toLowerCase(),attributes,children]}return sha256Text(canonicalJson((svg[0]?.children||[]).map(canonicalNode).filter(Boolean)))}async function iconAuthority(root,sourceFamily){const[authority,slots]=await Promise.all([readJson(path.join(root,"capabilities/runtime-authority-manifest.json"),"Runtime authority"),readJson(path.join(root,"capabilities/layouts/icon-slot-contracts.json"),"图标槽位合同")]);if(authority.format!=="wise-ppt-runtime-authority@4")throw new WisePPTError("Runtime authority 合同错误");if(slots.contract!=="wise-ppt-icon-slots@2")throw new WisePPTError("图标槽位合同错误");if(!ICON_SOURCE_FAMILIES.includes(sourceFamily))throw new WisePPTError(`主题图标源未登记: ${sourceFamily||"-"}`);if(canonicalJson(authority.icons?.source_families)!==canonicalJson(ICON_SOURCE_FAMILIES))throw new WisePPTError("Runtime authority 的主题图标源集合不闭合");const entries=authority.icons?.entries;if(!Array.isArray(entries)||entries.length!==authority.icons?.selectable_count)throw new WisePPTError("Catalog authority 图标登记数量不闭合");const allowed=slots.allowed_icon_scope||{};if(allowed.selectable_count!==entries.length||allowed.names_sha256!==sha256Text(entries.map(entry=>entry.name).join(`
`)))throw new WisePPTError("图标槽位合同的允许图标范围已过期");const allowedFamily=allowed.source_families?.[sourceFamily];if(!allowedFamily||typeof allowedFamily!="object")throw new WisePPTError(`图标槽位合同缺少主题源: ${sourceFamily}`);const allowedPaint=new Set(allowedFamily.paint||[]),allowedWidths=new Set((allowedFamily.stroke_widths||[]).map(Number)),rootPresentation=allowedFamily.root_presentation||{},result=new Map;for(const entry of entries){if(!entry||typeof entry!="object"||!/^[a-z0-9][a-z0-9-]*$/.test(String(entry.name||""))||result.has(entry.name))throw new WisePPTError(`Catalog authority 图标条目非法或重复: ${entry?.name||"-"}`);if(canonicalJson(Object.keys(entry.variants||{}).sort())!==canonicalJson([...ICON_SOURCE_FAMILIES].sort()))throw new WisePPTError(`Catalog authority 图标主题源不闭合: ${entry.name}`);const variant=entry.variants[sourceFamily],source=path.resolve(root,...String(variant?.source||"").split("/"));if(!source.startsWith(`${path.resolve(root)}${path.sep}`))throw new WisePPTError(`Catalog authority 图标越出仓库: ${variant?.source||"-"}`);if((await shaFile(source).catch(()=>null))?.sha256!==variant?.sha256)throw new WisePPTError(`Catalog authority 图标缺失或哈希错误: ${entry.name}/${sourceFamily}`);const sourceText=await readText(source,`本地图标 ${entry.name}`),source$=load(sourceText,{xmlMode:!0},!1),sourceSvg=source$("svg").first();if((sourceSvg.attr("viewBox")||sourceSvg.attr("viewbox"))!==allowedFamily.viewBox)throw new WisePPTError(`Catalog 本地图标 viewBox 越出槽位合同: ${entry.name}/${sourceFamily}`);for(const attribute of["fill","stroke"])source$(`[${attribute}]`).each((_index,node)=>{const value=String(node.attribs?.[attribute]||"");if(!allowedPaint.has(value))throw new WisePPTError(`Catalog 本地图标 ${attribute} 越出槽位合同: ${entry.name}/${sourceFamily}`)});source$("[stroke-width]").each((_index,node)=>{if(!allowedWidths.has(Number(node.attribs?.["stroke-width"])))throw new WisePPTError(`Catalog 本地图标线宽越出槽位合同: ${entry.name}/${sourceFamily}`)});for(const[attribute,expected]of Object.entries(rootPresentation))if(sourceSvg.attr(attribute)!==String(expected))throw new WisePPTError(`Catalog 本地图标根属性越出槽位合同: ${entry.name}/${sourceFamily}/${attribute}`);const bodyHtml=(sourceSvg.html()||"").replace(/<([a-z][a-z0-9:-]*)([^>]*)\/>/gi,"<$1$2></$1>");result.set(entry.name,{body_html:bodyHtml,body_sha256:svgBodyDigest(sourceSvg)})}return result.sourceFamily=sourceFamily,result.viewBox=allowedFamily.viewBox,result.rootPresentation=rootPresentation,result}function bindingsFor(seed,slotId,kind){const value=seed.bindings?.slots?.[slotId]?.[kind];if(!Array.isArray(value))throw new WisePPTError(`${seed.display_code}/${slotId} seed ${kind} 绑定非法`);return value}function characterCount(value){return[...String(value).replace(/\s/g,"")].length}function applyTextPayload($,stage,seed,slot,raw,label,payloadValue){const bindings=bindingsFor(seed,slot.slot_id,"text");if(!bindings.length)throw new WisePPTError(`${label} 没有锁定 text binding`);const{fields,items}=payloadFields(raw,bindings,label);if(validateCapacity(slot,items,label),Object.keys(fields).length&&items===0)throw new WisePPTError(`${label}.items=0 时不得填写字段`);const byKey=new Map(bindings.map(item=>[item.key,item]));for(const[key,value]of Object.entries(fields)){if(!["string","number"].includes(typeof value)||typeof value=="boolean"||typeof value=="number"&&!Number.isFinite(value))throw new WisePPTError(`${label}.${key} 必须是文字或数字`);if(typeof value=="string"&&!value.trim())throw new WisePPTError(`${label}.${key} 不得是空白文字`);const capacity=Number(byKey.get(key)?.max_chars||0);if(capacity&&characterCount(value)>capacity)throw new WisePPTError(`${label}.${key} 超过 ${capacity} 字；禁止缩字号强塞`);const target=stage.find(`[data-vnext-text-key="${key}"]`).first();if(!target.length)throw new WisePPTError(`${label}.${key} 未落到锁定 DOM`);replaceText($,target,value),target.attr("data-vnext-payload-value",payloadValue)}return items}function materializeRegisteredIcon(target,iconName,icons,label){const record=icons.get(iconName);if(!record)throw new WisePPTError(`${label} 不是 Catalog 已登记本地图标: ${iconName}`);if(String(target[0]?.name||"").toLowerCase()!=="svg")throw new WisePPTError(`${label} 未落到锁定 SVG 图标目标`);target.empty().removeAttr("data-icon").removeAttr("data-theme-icon-verified");for(const attribute of ICON_SOURCE_PRESENTATION_ATTRIBUTES)target.removeAttr(attribute);target.removeAttr("viewbox").attr("viewBox",icons.viewBox);for(const[attribute,value]of Object.entries(icons.rootPresentation))target.attr(attribute,String(value));target.attr("data-icon-source",`${icons.sourceFamily}:${iconName}`).append(record.body_html)}async function applyIconPayload($,stage,root,icons,seed,slot,raw,label){const bindings=bindingsFor(seed,slot.slot_id,"icon");if(!bindings.length)throw new WisePPTError(`${label} 没有锁定 icon binding`);const{fields,items}=payloadFields(raw,bindings,label);validateCapacity(slot,items,label);for(const[key,value]of Object.entries(fields)){const iconName=plainString(value,`${label}.${key}`);if(!/^[a-z0-9][a-z0-9-]*$/.test(iconName)||!icons.has(iconName))throw new WisePPTError(`${label}.${key} 不是 Catalog 已登记本地图标: ${iconName}`);const target=stage.find(`[data-vnext-icon-key="${key}"]`).first();if(!target.length)throw new WisePPTError(`${label}.${key} 未落到锁定 DOM`);materializeRegisteredIcon(target,iconName,icons,`${label}.${key}`),target.attr("data-vnext-payload-value","icon")}return items}function validateSourceThemeSemantics($,stage,label,{allowVerifiedIcons=!1}={}){const select=selector=>stage.find(selector).addBack(selector);if(!allowVerifiedIcons&&select("[data-theme-icon-verified]").length)throw new WisePPTError(`${label} 不得预置 data-theme-icon-verified；该标记只能由 standard 验证图标后投影`);for(const[attribute,allowed]of Object.entries(THEME_SEMANTIC_VALUES))select(`[${attribute}]`).each((_index,node)=>{const value=String(node.attribs?.[attribute]||"");if(!allowed.has(value))throw new WisePPTError(`${label} ${attribute} 非法: ${value||"-"}`)});select('[data-theme-layer="foreground"]').each((_index,node)=>{if(node.attribs?.["data-theme-surface"]!=="foreground")throw new WisePPTError(`${label} foreground layer 必须同时声明 foreground surface`)}),select('[data-theme-role="reverse"]').each((_index,node)=>{if(!$(node).closest('[data-theme-surface="ink"]').length)throw new WisePPTError(`${label} reverse 角色必须落在显式 ink surface 内`)}),select("[data-theme-table-region]").each((_index,node)=>{const attrs=node.attribs||{};if(!attrs["data-theme-surface"]||!attrs["data-theme-line"])throw new WisePPTError(`${label} 表格区域必须同时声明显式 surface 与 line，禁止按类名猜测`);if(attrs["data-theme-table-region"]==="selected"&&attrs["data-theme-role"])throw new WisePPTError(`${label} selected 表格状态不得另设 theme focus；逐页焦点只认 emphasis 合同`)}),select("svg[data-icon-source]").each((_index,node)=>{if(!$(node).closest("[data-vnext-icon-key]").length)throw new WisePPTError(`${label} data-icon-source 必须属于已登记 icon binding`)})}function iconSourceIdentity(raw){const match=/^([a-z0-9][a-z0-9.-]*):([a-z0-9][a-z0-9-]*)$/.exec(String(raw||""));return!match||!ICON_SOURCE_FAMILIES.includes(match[1])?null:{family:match[1],name:match[2]}}function projectVerifiedIcons($,stage,icons,label){validateSourceThemeSemantics($,stage,label),stage.find("svg[data-icon-source]").each((_index,node)=>{const svg=$(node),identity=iconSourceIdentity(svg.attr("data-icon-source"));if(!identity||!icons.has(identity.name))throw new WisePPTError(`${label} 图标来源未通过 Catalog registry/authority 验证: ${svg.attr("data-icon-source")||"-"}`);materializeRegisteredIcon(svg,identity.name,icons,`${label}.${identity.name}`),svg.attr("data-theme-icon-verified",icons.sourceFamily)})}function validateMaterializedThemeSemantics($,stage,icons,label){validateSourceThemeSemantics($,stage,label,{allowVerifiedIcons:!0}),stage.find("[data-theme-icon-verified]").addBack("[data-theme-icon-verified]").each((_index,node)=>{const carrier=$(node),identity=iconSourceIdentity(carrier.attr("data-icon-source"));if(String(node.name).toLowerCase()!=="svg"||carrier.attr("data-theme-icon-verified")!==icons.sourceFamily||identity?.family!==icons.sourceFamily||!icons.has(identity?.name))throw new WisePPTError(`${label} 含未验证或位置非法的 theme icon marker`)}),stage.find("svg[data-icon-source]").each((_index,node)=>{const svg=$(node),identity=iconSourceIdentity(svg.attr("data-icon-source")),iconName=identity?.name,actualBodySha256=svgBodyDigest(svg),expected=iconName&&icons.has(iconName)?icons.get(iconName):null,rootPresentationMatches=expected&&ICON_SOURCE_PRESENTATION_ATTRIBUTES.every(attribute=>{const expectedValue=icons.rootPresentation[attribute],actualValue=svg.attr(attribute);return expectedValue===void 0?actualValue===void 0:actualValue===String(expectedValue)});if(!iconName||identity.family!==icons.sourceFamily||!expected||svg.attr("data-theme-icon-verified")!==icons.sourceFamily||(svg.attr("viewBox")||svg.attr("viewbox"))!==icons.viewBox||!rootPresentationMatches||actualBodySha256!==expected.body_sha256)throw new WisePPTError(`${label} 登记图标 ${iconName||"-"} 的 marker 或 SVG 子树与 Catalog authority 不一致（${actualBodySha256} != ${expected?.body_sha256||"-"}）`)})}function escapeHtml(value,quote=!0){let result=String(value).replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;");return quote&&(result=result.replaceAll('"',"&quot;").replaceAll("'","&#x27;")),result}function expectedPayloadReceipt(slide){const result=[];for(const[category,payloadType]of Object.entries(PAYLOAD_CATEGORIES))for(const[slotId,raw]of Object.entries(slide.payload[category]||{}).sort(([left],[right])=>left.localeCompare(right,"en")))result.push({slot_id:slotId,payload_type:payloadType,item_count:raw&&typeof raw=="object"&&!Array.isArray(raw)?Number(raw.items??1):1});return result}function materializedComponentIds(stage){const ids=new Set;return stage.find("[data-component-id]").addBack("[data-component-id]").each((_index,node)=>{node.attribs?.["data-component-id"]&&ids.add(node.attribs["data-component-id"])}),[...ids].sort()}function zeroCounts(keys){return Object.fromEntries(keys.map(key=>[key,0]))}function expectedComponentReceipt(slide,layout,seed,stage){return{page_id:slide.page_id,layout_id:layout.layout_id,layout_seed_sha256:seed.seed_sha256,structure_sha256:seed.locked.stage_structure_sha256,core_component_ids:layout.locks?.core_component_ids||[],materialized_component_ids:materializedComponentIds(stage),payload:expectedPayloadReceipt(slide),forbidden_counts:zeroCounts(RECEIPT_FORBIDDEN_KEYS)}}function expectedGeometryContract(slide,layout,seed){return{page_id:slide.page_id,layout_id:layout.layout_id,canvas:{width:1920,height:1080},structure_fingerprint:layout.locks?.structure_fingerprint??null,stage_structure_sha256:seed.locked.stage_structure_sha256,locked_geometry:layout.locks?.slot_spaces||layout.locks?.geometry||{},screen_print_tolerance_px:1}}function normalizedVisibleText(value){return String(value).trim().replace(/\s+/g," ")}function validatePageEvidence($,container,slide,mustById){const visible=[];container.find('[data-vnext-payload-value="claim"], [data-vnext-payload-value="text"], [data-vnext-payload-value="data"]').addBack('[data-vnext-payload-value="claim"], [data-vnext-payload-value="text"], [data-vnext-payload-value="data"]').each((_index,node)=>{const text=normalizedVisibleText($(node).text());text&&visible.push(text)});const evidence=[];for(const[sourceId,terms]of Object.entries(slide.source_evidence))terms.forEach(term=>evidence.push([`source ${sourceId}`,term]));for(const mustId of slide.must_refs)evidence.push([`must ${mustId}`,mustById.get(mustId).visible_evidence]);for(const[label,raw]of evidence){const term=normalizedVisibleText(raw);if(!visible.some(text=>text.includes(term)))throw new WisePPTError(`${slide.page_id} 的 ${label} visible_evidence 未出现在真实 payload/claim 可见文字: ${raw}`)}}function cornerLines($,doc){const lines=["",""];let index=0;for(const child of doc[0].children||[]){if(child.type==="tag"&&String(child.name).toLowerCase()==="br"){index=1;continue}const text=child.type==="text"?child.data:$(child).text();lines[index]+=String(text||"").trim()}return lines}async function materializeSlide(root,slide,layout,seed,deckTitle,folioText,signature,icons,themeBinding){if(seed.layout_id!==layout.layout_id||canonicalJson(seed.source)!==canonicalJson(layout.source))throw new WisePPTError(`${layout.layout_id} seed 与注册表来源不一致`);const $=load(seed.locked.stage_html,null,!1),stage=$(".stage").first();if(!stage.length)throw new WisePPTError(`${layout.layout_id} seed 缺少 .stage`);if(structureDigest(stage.toString())!==seed.locked.stage_structure_sha256)throw new WisePPTError(`${layout.layout_id} seed 结构哈希漂移`);validateSourceThemeSemantics($,stage,`${layout.display_code} seed`);const slotMap=new Map((layout.slots||[]).map(slot=>[slot.slot_id,slot])),receipt=[];if(layout.page_kind==="relationship"){const claim=seed.bindings?.claim,target=stage.find(`[data-vnext-claim-key="${claim?.key||""}"]`);if(claim?.key!=="claim.text.001"||target.length!==1)throw new WisePPTError(`${slide.page_id} 的关系骨架缺少唯一 claim binding`);if(characterCount(slide.claim)>claim.max_chars)throw new WisePPTError(`${slide.page_id}.claim 超过骨架结论容量 ${claim.max_chars} 字`);replaceText($,target.first(),slide.claim),target.attr("data-vnext-payload-value","claim")}for(const[category,payloadType]of Object.entries(PAYLOAD_CATEGORIES))for(const[slotId,raw]of Object.entries(slide.payload[category]||{}).sort(([left],[right])=>left.localeCompare(right,"en"))){const slot=slotMap.get(slotId),label=`${slide.page_id}.payload.${category}.${slotId}`,count=category==="icons"?await applyIconPayload($,stage,root,icons,seed,slot,raw,label):applyTextPayload($,stage,seed,slot,raw,label,category);receipt.push({slot_id:slotId,payload_type:payloadType,item_count:count})}stage.find("[data-vnext-text-key]").each((_index,node)=>{const target=$(node);target.attr("data-vnext-payload-value")||(replaceText($,target,""),target.attr("data-vnext-payload-value","empty"))}),projectVerifiedIcons($,stage,icons,slide.page_id),validateMaterializedThemeSemantics($,stage,icons,slide.page_id);const doc=stage.find(".doc.tl").first();if(!doc.length)throw new WisePPTError(`${slide.page_id} 骨架缺少左上角 .doc.tl`);const[line1,line2]=cornerLines($,doc);if(line1!==deckTitle.trim())throw new WisePPTError(`${slide.page_id} 左上角第一行必须是整副标题 deck.title（${deckTitle}）；当前是 ${line1||"空"}`);if(line2!==slide.claim.trim())throw new WisePPTError(`${slide.page_id} 左上角第二行必须是本页标题 claim（${slide.claim}）；当前是 ${line2||"空"}`);const folios=stage.find(".folio");if(folios.length!==1)throw new WisePPTError(`${slide.page_id} 骨架缺少唯一左下角 folio；找到 ${folios.length} 个`);replaceText($,folios.first(),folioText);const signatures=stage.find('[data-template-part="signature"]');if(signatures.length>1)throw new WisePPTError(`骨架署名槽必须至多一个；找到 ${signatures.length} 个`);if(signatures.length&&replaceText($,signatures.first(),signature),structureDigest(stage.toString())!==seed.locked.stage_structure_sha256)throw new WisePPTError(`${slide.page_id} payload 改变了锁定 DOM/结构/组件/几何`);const idMap=prefixDomIds($,stage,safePagePrefix(slide.page_id));if(structureDigest(stage.toString())!==seed.locked.stage_structure_sha256)throw new WisePPTError(`${slide.page_id} ID 隔离改变了骨架结构`);if(projectLayoutThemeBindings(stage,idMap,themeBinding,slide.page_id),validateProjectedLayoutThemeBindings(stage,themeBinding,slide.page_id),validateMaterializedThemeSemantics($,stage,icons,slide.page_id),structureDigest(stage.toString())!==seed.locked.stage_structure_sha256)throw new WisePPTError(`${slide.page_id} 主题语义投影改变了锁定 DOM/结构/组件/几何`);const emphasis=materializedEmphasis(slide,layout,stage,idMap);if(canonicalJson(receipt)!==canonicalJson(expectedPayloadReceipt(slide)))throw new WisePPTError(`${slide.page_id} payload receipt 与实际 materialization 计数不一致`);const attributes={class:"slide","data-page-id":slide.page_id,"data-page-title":slide.claim,"data-page-summary":slide.claim,"data-page-role":slide.page_role,"data-page-kind":layout.page_kind,"data-layout-id":layout.layout_id,"data-layout-code":layout.display_code,"data-layout-source":"registered","data-layout-seed-sha256":seed.seed_sha256,"data-layout-structure-sha256":seed.locked.stage_structure_sha256,"data-section-id":String(slide.section_id||"section.default"),"data-section-title":String(slide.section_title||""),"data-source-refs":canonicalJson(slide.source_refs),"data-source-evidence":canonicalJson(slide.source_evidence),"data-must-refs":canonicalJson(slide.must_refs),"data-emphasis-mode":emphasis?"semantic-focus":"none"};return slide.relation_key&&(attributes["data-relation-key"]=slide.relation_key),emphasis&&(attributes["data-emphasis-target"]=emphasis.target,attributes["data-emphasis-ref"]=emphasis.ref,attributes["data-emphasis-reason"]=emphasis.reason,attributes["data-emphasis-roles"]=emphasis.roles.join(" "),attributes["data-emphasis-members"]=canonicalJson(emphasis.members)),{section:`<section ${Object.entries(attributes).map(([name,value])=>`${name}="${escapeHtml(value)}"`).join(" ")}>
${stage.toString()}
</section>`,css:rewriteScopedCss(seed.locked.scoped_css,layout.layout_id,slide.page_id,idMap),receipt:expectedComponentReceipt(slide,layout,seed,stage),geometry:expectedGeometryContract(slide,layout,seed)}}function mustOutcomes(spec){return spec.must.map(item=>({must_id:item.must_id,status:item.status,page_id:item.page_id??null,reason:item.reason??null,visible_evidence:item.visible_evidence??null,source_refs:item.source_refs||[]}))}async function copyAuthoritativeFiles(root,outputRoot,resolvedFonts){const mapping=new Map([...REQUIRED_THEME_FILES.map(relative=>[relative,`assets/${path.basename(relative)}`]),...REQUIRED_RUNTIME_FILES.map(relative=>[relative,relative])]),written=[];for(const[sourceRelative,outputRelative]of mapping.entries()){const source=path.join(root,...sourceRelative.split("/"));if(!await exists(source))throw new WisePPTError(`缺少权威运行时资产: ${sourceRelative}`);const output=path.join(outputRoot,...outputRelative.split("/"));sourceRelative===THEME_ENGINE_DESIGN_TOKENS?(await mkdir(path.dirname(output),{recursive:!0}),await writeFile(output,projectThemeAssetForPublishedAssets(sourceRelative,await readText(source,sourceRelative)))):await copyFileSafe(source,output),written.push(outputRelative)}return written.push(...await copyResolvedFonts(resolvedFonts,outputRoot)),written.sort()}async function currentCompilerHashes(root){const paths=[...(await readdir(path.join(root,"bin"),{withFileTypes:!0})).filter(entry=>entry.isFile()&&(entry.name.endsWith(".mjs")||entry.name==="package.json")).map(entry=>`bin/${entry.name}`).sort((left,right)=>left.localeCompare(right,"en")),"runtime/app-template.html"],result={};for(const relative of paths){const file=path.join(root,...relative.split("/")),digest=await shaFile(file).catch(()=>null);if(!digest)throw new WisePPTError(`缺少 Wise PPT 编译器文件: ${relative}`);result[relative]=digest.sha256}return result}async function currentAuthorityHashes(root){const result={};for(const relative of REQUIRED_THEME_FILES){const outputRelative=`assets/${path.basename(relative)}`,source=path.join(root,...relative.split("/"));result[outputRelative]=relative===THEME_ENGINE_DESIGN_TOKENS?sha256Text(projectThemeAssetForPublishedAssets(relative,await readText(source,relative))):(await shaFile(source)).sha256}for(const relative of REQUIRED_RUNTIME_FILES)result[relative]=(await shaFile(path.join(root,...relative.split("/")))).sha256;const manifest=await readJson(path.join(root,"themes/engine/fonts/font-manifest.json"),"font-manifest");for(const font of manifest.fonts||[])result[`assets/fonts/${font.filename}`]=font.sha256;return Object.fromEntries(Object.entries(result).sort(([a],[b])=>a.localeCompare(b,"en")))}async function currentCapabilityHashes(root){const paths=["capabilities/layouts/page-emphasis-contracts.json","capabilities/layouts/icon-slot-contracts.json","themes/engine/contracts/layout-theme-bindings.json","themes/engine/contracts/component-theme-bindings.json"],result={};for(const relative of paths)result[relative]=(await shaFile(path.join(root,...relative.split("/")))).sha256;return result}async function buildTo(root,specPath,outputRoot,options={}){const{registry,index,sha256:registrySha}=await registryState(root),themeBindings=await loadLayoutThemeBindings(root,new Set(index.keys())),spec=await readJson(specPath,"deck-spec"),resolved=await validateSpec(root,spec,index),appearance=await resolvedAppearance(root,spec.deck);await mkdir(outputRoot,{recursive:!0});const resolvedFonts=await resolveFonts(root,options.fonts||{});await copyAuthoritativeFiles(root,outputRoot,resolvedFonts);const icons=await iconAuthority(root,appearance.theme.icons.source_family),sections=[],cssBlocks=[],receipts=[],geometries=[],usedSeeds={},mustById=new Map(spec.must.map(item=>[item.must_id,item])),total=resolved.length,signature=String(spec.deck.signature??DEFAULT_SIGNATURE).trim();for(const[offset,{slide,layout}]of resolved.entries()){const seed=await loadSeed(root,layout.display_code),folioText=`${offset+1} / ${total}${signature?` — BY ${signature}`:""}`,built=await materializeSlide(root,slide,layout,seed,String(spec.deck.title),folioText,signature,icons,themeBindings.byLayout.get(layout.layout_id)),section$=load(built.section,null,!1);validatePageEvidence(section$,section$("section.slide").first(),slide,mustById),sections.push(projectThemeTypeRoles(built.section)),cssBlocks.push(`/* ${slide.page_id} · ${layout.layout_id} · locked */
${built.css}`),receipts.push(built.receipt),geometries.push(built.geometry),usedSeeds[layout.layout_id]=seed.seed_sha256}await mkdir(path.join(outputRoot,"assets"),{recursive:!0}),await writeFile(path.join(outputRoot,"assets/layouts.css"),`${cssBlocks.join(`
`).trimEnd()}
`);const canonicalSpec=structuredClone(spec);canonicalSpec.mode="standard",canonicalSpec.deck.theme.kind==="inline"&&(canonicalSpec.deck.theme.definition=appearance.theme.definition),await writeFile(path.join(outputRoot,"deck-spec.json"),renderJson(canonicalSpec)),await Promise.all([writeFile(path.join(outputRoot,"assets/theme.css"),renderThemeCss(appearance.theme)),writeFile(path.join(outputRoot,"theme-resolved.json"),renderJson(appearance.theme))]);const deckPlan={contract:DECK_PLAN_CONTRACT,title:spec.deck.title,thesis:spec.deck.thesis,input_type:spec.deck.input_type,must:structuredClone(spec.must),layout_session:structuredClone(resolved.layoutSession),pages:resolved.map(({slide,layout})=>({page_id:slide.page_id,page_role:slide.page_role,page_kind:layout.page_kind,relation_key:slide.relation_key??null,layout_id:layout.layout_id,claim:slide.claim,source_refs:slide.source_refs,source_evidence:structuredClone(slide.source_evidence),must_refs:slide.must_refs,emphasis:slide.emphasis?structuredClone(slide.emphasis):null,layout_override:slide.layout_override?structuredClone(slide.layout_override):null}))},sourceLedger={contract:"wise-ppt-source-ledger@4",input_type:spec.deck.input_type,sources:structuredClone(spec.sources),must_sources:spec.must.map(item=>({must_id:item.must_id,status:item.status,page_id:item.page_id??null,source_refs:item.source_refs})),page_sources:resolved.map(({slide})=>({page_id:slide.page_id,source_refs:slide.source_refs,source_evidence:structuredClone(slide.source_evidence),must_refs:slide.must_refs}))},componentReceipts={contract:"wise-ppt-component-receipts@3",pages:receipts},geometryContracts={contract:"wise-ppt-geometry-contracts@3",pages:geometries};await Promise.all([writeFile(path.join(outputRoot,"deck-plan.json"),renderJson(deckPlan)),writeFile(path.join(outputRoot,"source-ledger.json"),renderJson(sourceLedger)),writeFile(path.join(outputRoot,"component-receipts.json"),renderJson(componentReceipts)),writeFile(path.join(outputRoot,"geometry-contracts.json"),renderJson(geometryContracts))]);const inputDigest=sha256Text(canonicalJson(canonicalSpec)),authoritativeHashes=await currentAuthorityHashes(root),capabilityHashes=await currentCapabilityHashes(root),compilerHashes=await currentCompilerHashes(root),buildId=sha256Text(canonicalJson({spec:inputDigest,registry:registrySha,seeds:usedSeeds,authoritative_files:authoritativeHashes,capability_contracts:capabilityHashes,compiler_sources:compilerHashes,runtime_version:RUNTIME_VERSION,theme:appearance.theme,typography_mode:appearance.typography})),{theme,themeId,typography}=appearance;let html=await readText(path.join(root,"runtime/app-template.html"),"Wise PPT app 模板");const replacements={"{{LANG}}":escapeHtml(spec.deck.lang||"zh-CN"),"{{PAGE_TITLE}}":escapeHtml(spec.deck.title,!1),"{{DECK_TITLE_ATTR}}":escapeHtml(spec.deck.title),"{{THEME_ID}}":escapeHtml(themeId),"{{TYPOGRAPHY_MODE}}":escapeHtml(typography),"{{BUILD_ID}}":buildId,"{{LAYOUT_REGISTRY_VERSION}}":registrySha,"{{RUNTIME_VERSION}}":RUNTIME_VERSION,"{{FINAL_EMPHASIS}}":resolved.some(({slide})=>slide.emphasis)?"semantic-focus":"none","{{SLIDES}}":sections.join(`
`)};for(const[marker,value]of Object.entries(replacements))html=html.replaceAll(marker,value);const unresolved=[...new Set(html.match(/\{\{[A-Z0-9_]+\}\}/g)||[])];if(unresolved.length)throw new WisePPTError(`Wise PPT 模板仍有未替换变量: ${unresolved.sort().join(", ")}`);await writeFile(path.join(outputRoot,"index.html"),html),await writeFile(path.join(outputRoot,OUTPUT_MARKER),`${BUILD_CONTRACT}
`);const managedPaths=(await collectFiles(outputRoot,{includeHidden:!0,exclude:relative=>[OUTPUT_MARKER,"build-manifest.json"].includes(relative)})).sort((a,b)=>a.localeCompare(b,"en")),managedFiles=[];for(const relative of managedPaths)managedFiles.push(await fileRecord(outputRoot,relative));const assetsEntries=managedFiles.filter(item=>item.path.startsWith("assets/")),buildManifest={contract:BUILD_CONTRACT,build_id:buildId,input_type:spec.deck.input_type,must_outcomes:mustOutcomes(spec),input:{path:"deck-spec.json",sha256:(await shaFile(path.join(outputRoot,"deck-spec.json"))).sha256},layout_registry:{registry_id:registry.registry_id,sha256:registrySha,counts:registry.counts},layout_seeds:Object.fromEntries(Object.entries(usedSeeds).sort(([a],[b])=>a.localeCompare(b,"en"))),compiler_sources:compilerHashes,authoritative_files:authoritativeHashes,capability_contracts:capabilityHashes,runtime_version:RUNTIME_VERSION,theme:{kind:spec.deck.theme.kind,theme_id:theme.theme_id,definition_contract:theme.definition.contract,resolved_contract:theme.contract,resolved_path:"theme-resolved.json",resolved_sha256:(await shaFile(path.join(outputRoot,"theme-resolved.json"))).sha256,css_path:"assets/theme.css",css_sha256:(await shaFile(path.join(outputRoot,"assets/theme.css"))).sha256,typography_mode:typography},page_count:sections.length,determinism:{input_digest:inputDigest,html_sha256:(await shaFile(path.join(outputRoot,"index.html"))).sha256,plan_sha256:(await shaFile(path.join(outputRoot,"deck-plan.json"))).sha256,assets_sha256:sha256Text(canonicalJson(assetsEntries))},standard_mode_forbidden_counts:zeroCounts(BUILD_FORBIDDEN_KEYS),managed_files:managedFiles};return await writeFile(path.join(outputRoot,"build-manifest.json"),renderJson(buildManifest)),buildManifest}async function directoryEntries(root){const files=new Set,directories=new Set;async function visit(current){for(const entry of await readdir(current,{withFileTypes:!0})){const absolute=path.join(current,entry.name),relative=path.relative(root,absolute).split(path.sep).join("/");if(entry.isSymbolicLink())throw new WisePPTError(`Wise PPT 输出目录含不安全符号链接: ${relative}`);if(entry.isDirectory())directories.add(relative),await visit(absolute);else if(entry.isFile())files.add(relative);else throw new WisePPTError(`Wise PPT 输出目录含不支持的文件类型: ${relative}`)}}return await visit(root),{files,directories}}function validateManifestEntries(rawEntries,label){if(!Array.isArray(rawEntries))throw new WisePPTError(`${label}.managed_files 必须是完整数组`);const entries=new Map;return rawEntries.forEach((item,offset)=>{const itemLabel=`${label}.managed_files[${offset+1}]`;if(!item||typeof item!="object"||Object.keys(item).sort().join(",")!=="bytes,path,sha256")throw new WisePPTError(`${itemLabel} 字段非法`);const relative=item.path;if(typeof relative!="string"||!relative||relative.startsWith("/")||relative.includes("\\")||relative.split("/").some(part=>["",".",".."].includes(part)))throw new WisePPTError(`${itemLabel}.path 非法: ${relative}`);if(entries.has(relative))throw new WisePPTError(`${label}.managed_files 路径重复: ${relative}`);if(!/^[0-9a-f]{64}$/.test(String(item.sha256||""))||!Number.isInteger(item.bytes)||item.bytes<0)throw new WisePPTError(`${itemLabel} 哈希或大小非法`);entries.set(relative,item)}),entries}async function validatePreviousOutput(output,specPath){const{files}=await directoryEntries(output);if(!files.size)return;if(!files.has(OUTPUT_MARKER)||!files.has("build-manifest.json")){if(files.size!==1||!files.has("deck-spec.json"))throw new WisePPTError(`拒绝覆盖非 Wise PPT 生成目录: ${output}`);const[draft,source]=await Promise.all([readJson(path.join(output,"deck-spec.json"),"已有 deck-spec"),readJson(specPath,"输入 deck-spec")]);if(canonicalJson(draft)!==canonicalJson(source))throw new WisePPTError(`输出目录里的 deck-spec.json 与本次构建输入不一致，拒绝收编: ${output}`);return}if(await readText(path.join(output,OUTPUT_MARKER),OUTPUT_MARKER)!==`${BUILD_CONTRACT}
`)throw new WisePPTError(`${OUTPUT_MARKER} 合同标记错误，拒绝覆盖`);const previous=await readJson(path.join(output,"build-manifest.json"),"已有 build-manifest");if(previous.contract!==BUILD_CONTRACT)throw new WisePPTError(`拒绝覆盖未知生成目录: ${output}`);const entries=validateManifestEntries(previous.managed_files,"已有 build-manifest"),delivery=new Set([...files].filter(item=>["deck.pdf","delivery-manifest.json"].includes(item)));if(delivery.size===1)throw new WisePPTError("已有输出的 deck.pdf 与 delivery-manifest.json 必须成对存在，拒绝覆盖");const expected=new Set([...entries.keys(),OUTPUT_MARKER,"build-manifest.json",...delivery]);if(expected.size!==files.size||setDifference(expected,files).length||setDifference(files,expected).length)throw new WisePPTError("Wise PPT 已有输出目录文件集不闭合，含未知文件，拒绝覆盖");for(const[relative,record]of entries){if(relative==="deck-spec.json")continue;const digest=await shaFile(path.join(output,...relative.split("/")));if(digest.sha256!==record.sha256||digest.bytes!==record.bytes)throw new WisePPTError(`已有 Wise PPT 受管产物已漂移，拒绝覆盖: ${relative}`)}if(delivery.size){const record=(await readJson(path.join(output,"delivery-manifest.json"),"已有 delivery-manifest")).artifacts?.pdf,digest=await shaFile(path.join(output,"deck.pdf"));if(record?.path!=="deck.pdf"||record.sha256!==digest.sha256||record.bytes!==digest.bytes)throw new WisePPTError("已有 delivery-manifest 的 PDF 哈希或大小已漂移，拒绝覆盖")}}async function publishBuilt(tempRoot,output,specPath){await assertNoSymlinkComponents(output,"--out");const existing=await stat(output).catch(()=>null);if(existing?.isFile())throw new WisePPTError(`--out 必须是目录: ${output}`);existing&&await validatePreviousOutput(output,specPath),await mkdir(path.dirname(output),{recursive:!0});const backup=`${output}.backup-${process.pid}-${Date.now()}`;let movedOld=!1;try{existing&&(await rename(output,backup),movedOld=!0),await rename(tempRoot,output),movedOld&&await rm(backup,{recursive:!0,force:!0})}catch(error){throw await exists(output)&&await rm(output,{recursive:!0,force:!0}).catch(()=>{}),movedOld&&await exists(backup)&&await rename(backup,output).catch(()=>{}),new WisePPTError(`Wise PPT 原子发布失败，旧输出已回滚: ${error.message}`)}}function validateZeroCounts(value,keys,label){if(!value||typeof value!="object"||Array.isArray(value)||Object.keys(value).sort().join(",")!==[...keys].sort().join(","))throw new WisePPTError(`${label} 必须精确包含固定禁区计数`);if(Object.entries(value).filter(([,item])=>!Number.isInteger(item)||item!==0).length)throw new WisePPTError(`${label} 的值必须是整数 0`)}async function expectedManagedSet(root){const result=new Set(FIXED_GENERATED_FILES);REQUIRED_THEME_FILES.forEach(relative=>result.add(`assets/${path.basename(relative)}`)),REQUIRED_RUNTIME_FILES.forEach(relative=>result.add(relative));const manifest=await readJson(path.join(root,"themes/engine/fonts/font-manifest.json"),"font-manifest");for(const font of manifest.fonts||[])result.add(`assets/fonts/${font.filename}`);return result.add("assets/theme.css"),result.add("theme-resolved.json"),result}async function validateManagedClosure(root,deckRoot,build){const expectedManaged=await expectedManagedSet(root),entries=validateManifestEntries(build.managed_files,"build-manifest");if(expectedManaged.size!==entries.size||setDifference(expectedManaged,new Set(entries.keys())).length||setDifference(new Set(entries.keys()),expectedManaged).length)throw new WisePPTError("build-manifest 受管文件集不完整");const{files}=await directoryEntries(deckRoot),delivery=new Set([...files].filter(item=>["deck.pdf","delivery-manifest.json"].includes(item)));if(delivery.size===1)throw new WisePPTError("deck.pdf 与 delivery-manifest.json 必须成对存在");if(delivery.size&&(await readJson(path.join(deckRoot,"delivery-manifest.json"),"delivery-manifest")).artifacts?.pdf?.path!=="deck.pdf")throw new WisePPTError("delivery-manifest.artifacts.pdf.path 必须固定为 deck.pdf");const expectedDisk=new Set([...expectedManaged,OUTPUT_MARKER,"build-manifest.json",...delivery]);if(expectedDisk.size!==files.size||setDifference(expectedDisk,files).length||setDifference(files,expectedDisk).length)throw new WisePPTError("磁盘受管文件集不闭合");if(await readText(path.join(deckRoot,OUTPUT_MARKER),OUTPUT_MARKER)!==`${BUILD_CONTRACT}
`)throw new WisePPTError(`${OUTPUT_MARKER} 合同标记错误`);for(const[relative,record]of entries){const digest=await shaFile(path.join(deckRoot,...relative.split("/")));if(digest.sha256!==record.sha256||digest.bytes!==record.bytes)throw new WisePPTError(`冻结产物已漂移: ${relative}`)}}function validateProjectionPageSet(document,contract,expectedPageIds,label){if(document.contract!==contract||!Array.isArray(document.pages))throw new WisePPTError(`${label} 合同或 pages 错误`);const pageIds=document.pages.map(item=>item?.page_id);if(pageIds.some(item=>typeof item!="string")||new Set(pageIds).size!==pageIds.length)throw new WisePPTError(`${label} page_id 非法或重复`);const expected=new Set(expectedPageIds),actual=new Set(pageIds);if(expected.size!==actual.size||setDifference(expected,actual).length||setDifference(actual,expected).length)throw new WisePPTError(`${label} 页集合不完整`);return document.pages}async function validateDeck(root,rawDeck){const deckRoot=path.resolve(rawDeck),required=["index.html","deck-spec.json","deck-plan.json","source-ledger.json","component-receipts.json","geometry-contracts.json","build-manifest.json",OUTPUT_MARKER],missing=[];for(const relative of required)await exists(path.join(deckRoot,relative))||missing.push(relative);if(missing.length)throw new WisePPTError(`Wise PPT deck 缺少产物: ${missing.join(", ")}`);const{registry,index,sha256:registrySha}=await registryState(root),themeBindings=await loadLayoutThemeBindings(root,new Set(index.keys())),spec=await readJson(path.join(deckRoot,"deck-spec.json"),"deck-spec"),resolved=await validateSpec(root,spec,index),appearance=await resolvedAppearance(root,spec.deck),icons=await iconAuthority(root,appearance.theme.icons.source_family),build=await readJson(path.join(deckRoot,"build-manifest.json"),"build-manifest");if(build.contract!==BUILD_CONTRACT)throw new WisePPTError("build-manifest 合同错误");if(build.layout_registry?.sha256!==registrySha||canonicalJson(build.layout_registry?.counts)!==canonicalJson(registry.counts))throw new WisePPTError("build 使用的骨架注册表已过期");if(build.page_count!==resolved.length||build.input_type!==spec.deck.input_type)throw new WisePPTError("build page_count/input_type 与 deck-spec 不一致");if(canonicalJson(build.must_outcomes)!==canonicalJson(mustOutcomes(spec)))throw new WisePPTError("build must_outcomes 与 deck-spec 不一致");if(build.runtime_version!==RUNTIME_VERSION)throw new WisePPTError(`build runtime_version 必须是 ${RUNTIME_VERSION}`);const expectedResolved=appearance.theme,actualResolved=await readJson(path.join(deckRoot,"theme-resolved.json"),"theme-resolved"),actualThemeCss=await readText(path.join(deckRoot,"assets/theme.css"),"theme.css");if(canonicalJson(actualResolved)!==canonicalJson(expectedResolved))throw new WisePPTError("theme-resolved.json 不是 deck.theme 的确定性投影");if(actualThemeCss!==renderThemeCss(expectedResolved))throw new WisePPTError("assets/theme.css 不是 deck.theme 的确定性投影");const expectedThemeManifest={kind:spec.deck.theme.kind,theme_id:expectedResolved.theme_id,definition_contract:expectedResolved.definition.contract,resolved_contract:expectedResolved.contract,resolved_path:"theme-resolved.json",resolved_sha256:(await shaFile(path.join(deckRoot,"theme-resolved.json"))).sha256,css_path:"assets/theme.css",css_sha256:(await shaFile(path.join(deckRoot,"assets/theme.css"))).sha256,typography_mode:appearance.typography};if(canonicalJson(build.theme)!==canonicalJson(expectedThemeManifest))throw new WisePPTError("build-manifest.theme 与锁定主题不一致");if(validateZeroCounts(build.standard_mode_forbidden_counts,BUILD_FORBIDDEN_KEYS,"build-manifest.standard_mode_forbidden_counts"),await validateManagedClosure(root,deckRoot,build),canonicalJson(build.compiler_sources)!==canonicalJson(await currentCompilerHashes(root)))throw new WisePPTError("build 使用的 Wise PPT 编译器/模板已过期；请重新 build");if(canonicalJson(build.authoritative_files)!==canonicalJson(await currentAuthorityHashes(root)))throw new WisePPTError("build 使用的主题/字体/runtime 已过期；请重新 build");if(canonicalJson(build.capability_contracts)!==canonicalJson(await currentCapabilityHashes(root)))throw new WisePPTError("build 使用的主题绑定、强调或图标槽位合同已过期；请重新 build");const deckPlan=await readJson(path.join(deckRoot,"deck-plan.json"),"deck-plan"),expectedPlanPages=resolved.map(({slide,layout})=>({page_id:slide.page_id,page_role:slide.page_role,page_kind:layout.page_kind,relation_key:slide.relation_key??null,layout_id:layout.layout_id,claim:slide.claim,source_refs:slide.source_refs,source_evidence:slide.source_evidence,must_refs:slide.must_refs,emphasis:slide.emphasis??null,layout_override:slide.layout_override??null}));if(deckPlan.contract!==DECK_PLAN_CONTRACT||deckPlan.input_type!==spec.deck.input_type||canonicalJson(deckPlan.must)!==canonicalJson(spec.must)||canonicalJson(deckPlan.layout_session)!==canonicalJson(resolved.layoutSession)||canonicalJson(deckPlan.pages)!==canonicalJson(expectedPlanPages))throw new WisePPTError("deck-plan 与 deck-spec 不一致");const sourceLedger=await readJson(path.join(deckRoot,"source-ledger.json"),"source-ledger"),expectedMustSources=spec.must.map(item=>({must_id:item.must_id,status:item.status,page_id:item.page_id??null,source_refs:item.source_refs})),expectedPageSources=resolved.map(({slide})=>({page_id:slide.page_id,source_refs:slide.source_refs,source_evidence:slide.source_evidence,must_refs:slide.must_refs}));if(sourceLedger.contract!=="wise-ppt-source-ledger@4"||sourceLedger.input_type!==spec.deck.input_type||canonicalJson(sourceLedger.sources)!==canonicalJson(spec.sources)||canonicalJson(sourceLedger.must_sources)!==canonicalJson(expectedMustSources)||canonicalJson(sourceLedger.page_sources)!==canonicalJson(expectedPageSources))throw new WisePPTError("source-ledger 与 deck-spec 来源/must 去向不一致");const html=await readText(path.join(deckRoot,"index.html"),"index.html"),$=load(html),htmlRoot=$("html").first();if(htmlRoot.attr("data-deck-contract-version")!=="9"||htmlRoot.attr("data-runtime-version")!==RUNTIME_VERSION)throw new WisePPTError("index.html deck/runtime 合同错误");if(htmlRoot.attr("data-theme-id")!==appearance.themeId)throw new WisePPTError("index.html theme id 与 deck-spec 不一致");if($('link[rel="stylesheet"][href="assets/theme.css"]').length!==1)throw new WisePPTError("index.html 必须且只能加载一个 assets/theme.css");if(htmlRoot.attr("data-theme-preset")!==void 0||htmlRoot.attr("data-theme-family")!==void 0)throw new WisePPTError("deck@9 HTML 不得残留旧主题接口");const expectedFinalEmphasis=resolved.some(({slide})=>slide.emphasis)?"semantic-focus":"none";if(htmlRoot.attr("data-final-emphasis")!==expectedFinalEmphasis)throw new WisePPTError("index.html 最终强调状态与 deck-spec 不一致");if(htmlRoot.attr("data-build-id")!==build.build_id||htmlRoot.attr("data-layout-registry-version")!==registrySha)throw new WisePPTError("index.html build/registry 元数据不一致");if(!$('link[rel="stylesheet"][href="runtime/deck-shell.css"]').length)throw new WisePPTError("index.html 未加载 HTML/PDF 共用 deck-shell.css");const slideNodes=$("#track > .slide").toArray();if(slideNodes.length!==resolved.length)throw new WisePPTError("index.html slide 数与 deck-spec 不一致");const expectedPageIds=resolved.map(({slide})=>slide.page_id),receipts=await readJson(path.join(deckRoot,"component-receipts.json"),"component-receipts");validateProjectionPageSet(receipts,"wise-ppt-component-receipts@3",expectedPageIds,"component-receipts").forEach((page,indexValue)=>validateZeroCounts(page.forbidden_counts,RECEIPT_FORBIDDEN_KEYS,`component-receipts.pages[${indexValue+1}].forbidden_counts`));const geometry=await readJson(path.join(deckRoot,"geometry-contracts.json"),"geometry-contracts");validateProjectionPageSet(geometry,"wise-ppt-geometry-contracts@3",expectedPageIds,"geometry-contracts");const expectedReceipts=[],expectedGeometries=[],expectedCss=[],mustById=new Map(spec.must.map(item=>[item.must_id,item]));for(let offset=0;offset<resolved.length;offset+=1){const{slide,layout}=resolved[offset],node=$(slideNodes[offset]);if(node.attr("data-page-id")!==slide.page_id||node.attr("data-layout-id")!==layout.layout_id||node.attr("data-layout-source")!=="registered")throw new WisePPTError(`${slide.page_id} HTML 元数据与 spec 不一致`);if(node.attr("data-source-refs")!==canonicalJson(slide.source_refs)||node.attr("data-source-evidence")!==canonicalJson(slide.source_evidence)||node.attr("data-must-refs")!==canonicalJson(slide.must_refs))throw new WisePPTError(`${slide.page_id} HTML 来源元数据与 spec 不一致`);if(slide.emphasis){const seedForEmphasis=await loadSeed(root,layout.display_code),seedForEmphasis$=load(seedForEmphasis.locked.stage_html,null,!1),seedForEmphasisStage=seedForEmphasis$(".stage").first(),emphasisIdMap=prefixDomIds(seedForEmphasis$,seedForEmphasisStage,safePagePrefix(slide.page_id)),expectedEmphasis=materializedEmphasis(slide,layout,seedForEmphasisStage,emphasisIdMap);if(node.attr("data-emphasis-mode")!=="semantic-focus"||node.attr("data-emphasis-target")!==expectedEmphasis.target||node.attr("data-emphasis-ref")!==expectedEmphasis.ref||node.attr("data-emphasis-reason")!==expectedEmphasis.reason||node.attr("data-emphasis-roles")!==expectedEmphasis.roles.join(" ")||node.attr("data-emphasis-members")!==canonicalJson(expectedEmphasis.members))throw new WisePPTError(`${slide.page_id} HTML 强调元数据与 spec/逐页合同不一致`)}else if(node.attr("data-emphasis-mode")!=="none"||node.attr("data-emphasis-target")||node.attr("data-emphasis-members"))throw new WisePPTError(`${slide.page_id} 未声明强调却残留强调元数据`);if(validatePageEvidence($,node,slide,mustById),node.find("style").length)throw new WisePPTError(`${slide.page_id} 含页面级 CSS`);const seed=await loadSeed(root,layout.display_code),stage=node.children(".stage").first(),stageDigest=stage.length?structureDigest(stage.toString()):null;if(!stage.length||stageDigest!==seed.locked.stage_structure_sha256)throw new WisePPTError(`${slide.page_id} DOM/CSS/结构/组件/几何已偏离锁定 seed（${stageDigest||"missing"} != ${seed.locked.stage_structure_sha256}）`);validateMaterializedThemeSemantics($,stage,icons,slide.page_id),validateProjectedLayoutThemeBindings(stage,themeBindings.byLayout.get(layout.layout_id),slide.page_id);const seed$=load(seed.locked.stage_html,null,!1),seedStage=seed$(".stage").first(),idMap=prefixDomIds(seed$,seedStage,safePagePrefix(slide.page_id));expectedCss.push(`/* ${slide.page_id} · ${layout.layout_id} · locked */
${rewriteScopedCss(seed.locked.scoped_css,layout.layout_id,slide.page_id,idMap)}`),expectedReceipts.push(expectedComponentReceipt(slide,layout,seed,stage)),expectedGeometries.push(expectedGeometryContract(slide,layout,seed))}if(canonicalJson(receipts)!==canonicalJson({contract:"wise-ppt-component-receipts@3",pages:expectedReceipts}))throw new WisePPTError("component-receipts 不是确定性完整投影");if(canonicalJson(geometry)!==canonicalJson({contract:"wise-ppt-geometry-contracts@3",pages:expectedGeometries}))throw new WisePPTError("geometry-contracts 不是确定性完整投影");if(await readText(path.join(deckRoot,"assets/layouts.css"),"assets/layouts.css")!==`${expectedCss.join(`
`).trimEnd()}
`)throw new WisePPTError("assets/layouts.css 不是锁定 seed 的确定性投影");return{page_count:slideNodes.length,build_id:build.build_id,registry_sha256:registrySha}}async function buildAndPublish(root,rawSpec,rawOutput,options={}){const specPath=assertAbsolute(rawSpec,"deck-spec 路径"),output=assertAbsolute(rawOutput,"--out ");if(!await exists(specPath))throw new WisePPTError(`deck-spec 不存在: ${specPath}`);await assertNoSymlinkComponents(specPath,"deck-spec"),await assertNoSymlinkComponents(output,"--out"),await mkdir(path.dirname(output),{recursive:!0});const tempRoot=await(await import("node:fs/promises")).mkdtemp(path.join(path.dirname(output),".wise-ppt-build-"));let published=!1;try{const manifest=await buildTo(root,specPath,tempRoot,options);return await validateDeck(root,tempRoot),await publishBuilt(tempRoot,output,specPath),published=!0,await validateDeck(root,output),{manifest,output}}finally{published||await rm(tempRoot,{recursive:!0,force:!0}).catch(()=>{})}}export{buildAndPublish,buildTo,layoutSelectionState,loadLayoutThemeBindings,loadSeed,normalizeLayoutContext,normalizeLayoutUsage,normalizeSelectionSeed,planLayouts,preflightSpec,projectLayoutThemeBindings,projectThemeTypeRoles,publishBuilt,queryLayouts,registryState,resolvedAppearance,structureDigest,validateDeck,validateSpec};
