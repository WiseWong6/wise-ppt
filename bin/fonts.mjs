import{copyFile,mkdir,open,readdir,rename,rm,stat,writeFile}from"node:fs/promises";import{spawnSync}from"node:child_process";import os from"node:os";import path from"node:path";import{shaFile,readJson,WisePPTError}from"./common.mjs";async function loadFontManifest(root){const manifestPath=path.join(root,"themes/engine/fonts/font-manifest.json"),manifest=await readJson(manifestPath,"font-manifest");if(manifest.contract_version!==4||!Array.isArray(manifest.fonts)||!manifest.fonts.length)throw new WisePPTError("font-manifest 合同错误");for(const font of manifest.fonts)if(!font.filename||!Array.isArray(font.system_filenames)||!font.system_filenames.length||!Array.isArray(font.system_family_names)||!font.system_family_names.length||!Number.isInteger(font.weight)||font.weight<100||font.weight>900)throw new WisePPTError(`font-manifest 字体家族或字重错误: ${font.filename||"unknown"}`);const digest=(await shaFile(manifestPath)).sha256;return{manifest,manifestPath,digest}}function fontCacheRoot(manifestDigest,env=process.env,platform=process.platform){let base;if(platform==="darwin")base=path.join(os.homedir(),"Library","Caches","wise-ppt","fonts");else if(platform==="win32"){const local=env.LOCALAPPDATA;if(!local)throw new WisePPTError("Windows 缺少 LOCALAPPDATA，无法确定 Wise PPT 字体缓存");base=path.join(local,"WisePPT","Cache","fonts")}else throw new WisePPTError(`Wise PPT 仅支持 macOS 和 Windows，当前平台：${platform}`);return path.join(base,manifestDigest)}function systemFontDirectories(env=process.env,platform=process.platform){if(platform==="darwin")return[path.join(os.homedir(),"Library","Fonts"),"/Library/Fonts","/System/Library/Fonts"];if(platform==="win32")return[env.WINDIR?path.join(env.WINDIR,"Fonts"):null,env.LOCALAPPDATA?path.join(env.LOCALAPPDATA,"Microsoft","Windows","Fonts"):null].filter(Boolean);throw new WisePPTError(`Wise PPT 仅支持 macOS 和 Windows，当前平台：${platform}`)}async function collectFontFiles(root,output,depth=0){if(depth>8)return;const entries=await readdir(root,{withFileTypes:!0}).catch(()=>[]);for(const entry of entries)entry.isFile()&&/\.(?:otf|ttf|ttc)$/i.test(entry.name)&&output.push(path.join(root,entry.name));for(const entry of entries)entry.isDirectory()&&await collectFontFiles(path.join(root,entry.name),output,depth+1)}function normalizedFontLabel(value){return String(value||"").toLocaleLowerCase("en-US").replace(/\([^)]*\)/g," ").replace(/[^\p{L}\p{N}]+/gu," ").trim()}function systemFontLabelMatches(font,label){const normalized=normalizedFontLabel(label);return font.system_family_names.some(name=>normalized.includes(normalizedFontLabel(name)))?({300:["light"],400:["regular","normal"],500:["medium"],700:["bold"]}[font.weight]||[String(font.weight)]).some(style=>normalized.includes(style))?!0:font.weight!==400?!1:!/(?:thin|light|medium|semibold|demibold|bold|black|heavy)/.test(normalized):!1}function decodeFontName(bytes,platformId){if(platformId===0||platformId===3){let output="";for(let offset=0;offset+1<bytes.length;offset+=2)output+=String.fromCharCode(bytes.readUInt16BE(offset));return output.replaceAll("\0","").trim()}return bytes.toString("latin1").replaceAll("\0","").trim()}async function readAt(handle,length,position){const bytes=Buffer.alloc(length),{bytesRead}=await handle.read(bytes,0,length,position);if(bytesRead!==length)throw new Error("字体表已截断");return bytes}async function sfntFaceOffsets(handle){const header=await readAt(handle,12,0);if(header.toString("ascii",0,4)!=="ttcf")return[0];const count=header.readUInt32BE(8);if(!count||count>256)throw new Error("TTC face 数量非法");const offsets=await readAt(handle,count*4,12);return Array.from({length:count},(_unused,index)=>offsets.readUInt32BE(index*4))}async function readSfntFace(handle,faceOffset){const header=await readAt(handle,12,faceOffset),signature=header.toString("ascii",0,4);if(!["OTTO","true","typ1"].includes(signature)&&header.readUInt32BE(0)!==65536)return null;const tableCount=header.readUInt16BE(4);if(!tableCount||tableCount>512)return null;const directory=await readAt(handle,tableCount*16,faceOffset+12),tables=new Map;for(let index=0;index<tableCount;index+=1){const offset=index*16;tables.set(directory.toString("ascii",offset,offset+4),{offset:directory.readUInt32BE(offset+8),length:directory.readUInt32BE(offset+12)})}const name=tables.get("name");if(!name||name.length<6||name.length>4*1024*1024)return null;const nameBytes=await readAt(handle,name.length,name.offset),count=nameBytes.readUInt16BE(2),strings=nameBytes.readUInt16BE(4);if(6+count*12>nameBytes.length)return null;const values=new Map;for(let index=0;index<count;index+=1){const offset=6+index*12,platformId=nameBytes.readUInt16BE(offset),languageId=nameBytes.readUInt16BE(offset+4),nameId=nameBytes.readUInt16BE(offset+6);if(![1,2,16,17].includes(nameId))continue;const length=nameBytes.readUInt16BE(offset+8),stringOffset=strings+nameBytes.readUInt16BE(offset+10);if(stringOffset+length>nameBytes.length)continue;const value=decodeFontName(nameBytes.subarray(stringOffset,stringOffset+length),platformId);if(!value)continue;const priority=(platformId===3?4:platformId===0?3:1)+([1033,0].includes(languageId)?1:0),current=values.get(nameId);(!current||priority>current.priority)&&values.set(nameId,{value,priority})}const os2=tables.get("OS/2");let weight=null;return os2?.length>=6&&(weight=(await readAt(handle,2,os2.offset+4)).readUInt16BE(0)),{family:values.get(16)?.value||values.get(1)?.value||"",style:values.get(17)?.value||values.get(2)?.value||"",weight}}async function readSfntFaces(file){const handle=await open(file,"r").catch(()=>null);if(!handle)return[];try{const offsets=await sfntFaceOffsets(handle),faces=[];for(const offset of offsets){const face=await readSfntFace(handle,offset).catch(()=>null);face&&faces.push(face)}return faces}catch{return[]}finally{await handle.close().catch(()=>{})}}function systemFontFaceMatches(font,face){const family=normalizedFontLabel(face.family);return font.system_family_names.some(name=>family===normalizedFontLabel(name))?Number.isInteger(face.weight)?face.weight===font.weight:systemFontLabelMatches(font,`${face.family} ${face.style}`):!1}async function windowsRegistryFont(font,env){const roots=["HKLM\\SOFTWARE\\Microsoft\\Windows NT\\CurrentVersion\\Fonts","HKCU\\SOFTWARE\\Microsoft\\Windows NT\\CurrentVersion\\Fonts"];for(const registryRoot of roots){const result=spawnSync("reg.exe",["query",registryRoot],{encoding:"utf8",windowsHide:!0,timeout:15e3});if(result.status===0)for(const line of String(result.stdout||"").split(/\r?\n/)){const match=line.match(/^\s*(.*?)\s+REG_(?:EXPAND_)?SZ\s+(.+?)\s*$/i);if(!match||!systemFontLabelMatches(font,match[1]))continue;const raw=match[2].replace(/%([^%]+)%/g,(_whole,key)=>env[key]||env[key.toUpperCase()]||""),candidates=path.isAbsolute(raw)?[raw]:[env.WINDIR?path.join(env.WINDIR,"Fonts",raw):null,env.LOCALAPPDATA?path.join(env.LOCALAPPDATA,"Microsoft","Windows","Fonts",raw):null].filter(Boolean);for(const candidate of candidates)if((await stat(candidate).catch(()=>null))?.isFile())return candidate}}return null}async function matchingSystemFonts(fonts,directories,options={}){const files=[];for(const directory of directories)await collectFontFiles(directory,files);const uniqueFiles=[...new Set(files)],byFilename=new Map(uniqueFiles.map(file=>[path.basename(file).toLocaleLowerCase("en-US"),file])),matches=new Map;for(const font of fonts){const candidate=(font.system_filenames||[font.filename]).map(name=>byFilename.get(name.toLocaleLowerCase("en-US"))).find(Boolean);candidate&&matches.set(font.filename,candidate)}if((options.platform||process.platform)==="win32")for(const font of fonts){if(matches.has(font.filename))continue;const candidate=await windowsRegistryFont(font,options.env||process.env);candidate&&matches.set(font.filename,candidate)}const unresolved=fonts.filter(font=>!matches.has(font.filename));if(!unresolved.length)return matches;for(const file of uniqueFiles){const faces=await readSfntFaces(file);for(const font of unresolved)!matches.has(font.filename)&&faces.some(face=>systemFontFaceMatches(font,face))&&matches.set(font.filename,file);if(unresolved.every(font=>matches.has(font.filename)))break}return matches}async function downloadFont(font,target,{retries=2,timeoutMs=6e5,fetchImpl=globalThis.fetch}={}){await mkdir(path.dirname(target),{recursive:!0});const errors=[];for(const url of font.urls||[])for(let attempt=1;attempt<=retries+1;attempt+=1){const temporary=`${target}.part-${process.pid}-${Date.now()}`;try{const controller=new AbortController,timer=setTimeout(()=>controller.abort(),timeoutMs);try{const response=await fetchImpl(url,{redirect:"follow",signal:controller.signal});if(!response.ok)throw new Error(`HTTP ${response.status}`);const bytes=Buffer.from(await response.arrayBuffer());await writeFile(temporary,bytes,{flag:"wx"})}finally{clearTimeout(timer)}const digest=await shaFile(temporary);if(digest.sha256!==font.sha256)throw new Error(`SHA-256 ${digest.sha256} != ${font.sha256}`);return await rename(temporary,target),target}catch(error){errors.push(`${url} [${attempt}/${retries+1}]: ${error.message}`)}finally{await rm(temporary,{force:!0}).catch(()=>{})}}throw new WisePPTError(`${font.filename} 所有下载源均失败：${errors.join("；")}`)}async function inspectFonts(root,options={}){const{manifest,digest}=await loadFontManifest(root),cacheRoot=fontCacheRoot(digest,options.env,options.platform),directories=options.systemDirectories??systemFontDirectories(options.env,options.platform),cacheDigests=new Map;for(const font of manifest.fonts)cacheDigests.set(font.filename,await shaFile(path.join(cacheRoot,font.filename)).catch(()=>null));const unresolved=manifest.fonts.filter(font=>cacheDigests.get(font.filename)?.sha256!==font.sha256),systemMatches=await matchingSystemFonts(unresolved,directories,options),records=[];for(const font of manifest.fonts){const cachePath=path.join(cacheRoot,font.filename),identity={filename:font.filename,family:font.system_family_names[0],weight:font.weight};if(cacheDigests.get(font.filename)?.sha256===font.sha256){records.push({...identity,status:"cache",path:cachePath,sha256:font.sha256});continue}const systemPath=systemMatches.get(font.filename);systemPath?records.push({...identity,status:"system",path:systemPath,sha256:null}):records.push({...identity,status:"missing",path:cachePath,sha256:font.sha256})}return{cacheRoot,manifestDigest:digest,records}}async function resolveFonts(root,options={}){const{manifest,digest}=await loadFontManifest(root),cacheRoot=fontCacheRoot(digest,options.env,options.platform);await mkdir(cacheRoot,{recursive:!0});const directories=options.systemDirectories??systemFontDirectories(options.env,options.platform),sources=new Map,unresolved=[];for(const font of manifest.fonts){const target=path.join(cacheRoot,font.filename),cached=await shaFile(target).catch(()=>null);if(cached?.sha256===font.sha256){sources.set(font.filename,{source:target,origin:"cache"});continue}cached&&await rm(target,{force:!0});const developmentSource=path.join(root,...String(font.asset||"").split("/"));(options.useDevelopmentSources===!1?null:await shaFile(developmentSource).catch(()=>null))?.sha256===font.sha256?sources.set(font.filename,{source:developmentSource,origin:"development-source"}):unresolved.push(font)}const systemMatches=await matchingSystemFonts(unresolved,directories,options),records=[];for(const font of manifest.fonts){const target=path.join(cacheRoot,font.filename),prepared=sources.get(font.filename);if(prepared?.origin==="cache"){records.push({font,...prepared});continue}if(prepared?.origin==="development-source"){const temporary=`${target}.part-${process.pid}-${Date.now()}`;await copyFile(prepared.source,temporary),await rename(temporary,target),records.push({font,source:target,origin:"development-source"});continue}const systemPath=systemMatches.get(font.filename);if(systemPath){records.push({font,source:systemPath,origin:"system"});continue}try{await downloadFont(font,target,options)}catch(error){throw new WisePPTError(`${error.message}。缓存目录：${cacheRoot}。联网后重新执行 build。`)}records.push({font,source:target,origin:"download"})}return{cacheRoot,manifestDigest:digest,records}}async function copyResolvedFonts(resolved,outputRoot){const written=[];for(const{font,source}of resolved.records){const relative=`assets/fonts/${font.filename}`,target=path.join(outputRoot,...relative.split("/"));await mkdir(path.dirname(target),{recursive:!0}),await copyFile(source,target),written.push(relative)}return written.sort()}export{copyResolvedFonts,fontCacheRoot,inspectFonts,loadFontManifest,resolveFonts,systemFontDirectories,systemFontFaceMatches,systemFontLabelMatches};
